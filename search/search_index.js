var __index = {"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"index.html","title":"Docs","text":"<p>This site contains the project documentation and serves as your starting point for working with the <code>CaloCem</code> package made available  on PyPI.</p>"},{"location":"index.html#table-of-contents","title":"Table Of Contents","text":"<p>The documentation consists of the following parts:</p> <ul> <li>How to Plot</li> <li>How to do a Tian Correction</li> <li>How to calculate characteristic values</li> <li>An Older How-To Guide</li> <li>Reference</li> </ul> Warning <p><code>CaloCem</code> has been developed without involvement of TA Instruments and is thus independent from the company  and its software.</p>"},{"location":"example_get_mainpeak_params.html","title":"Extracting Main-Peak Parameters","text":"In\u00a0[1]: Copied! <pre>from pathlib import Path\n\nimport matplotlib.pyplot as plt\n\nfrom calocem import Measurement, ProcessingParameters\n\n# Paths are relative to this notebook's directory (docs/).\ndatapath = Path(\"../calocem/DATA\")\nmetadatapath = Path(\"../calocem/METADATA/metadata_dummy.csv\")\n\ntam = Measurement(\n    folder=datapath,\n    regex=r\".*peak_detection_example2.*\",\n    show_info=False,\n    metadata_path=metadatapath,\n    metadata_id_column=\"file_name\",\n)\n</pre> from pathlib import Path  import matplotlib.pyplot as plt  from calocem import Measurement, ProcessingParameters  # Paths are relative to this notebook's directory (docs/). datapath = Path(\"../calocem/DATA\") metadatapath = Path(\"../calocem/METADATA/metadata_dummy.csv\")  tam = Measurement(     folder=datapath,     regex=r\".*peak_detection_example2.*\",     show_info=False,     metadata_path=metadatapath,     metadata_id_column=\"file_name\", ) <pre>\n---------------------------------------------------------------------------\nModuleNotFoundError                       Traceback (most recent call last)\nCell In[1], line 3\n      1 from pathlib import Path\n      2 \n----&gt; 3 import matplotlib.pyplot as plt\n      4 \n      5 from calocem import Measurement, ProcessingParameters\n      6 \n\nModuleNotFoundError: No module named 'matplotlib'</pre> In\u00a0[2]: Copied! <pre>mainpeak = tam.get_mainpeak_params(show_plot=True, plot_type=\"mean\")\nsorted_cols = sorted(mainpeak.columns)\ndisplay(sorted_cols)\n</pre> mainpeak = tam.get_mainpeak_params(show_plot=True, plot_type=\"mean\") sorted_cols = sorted(mainpeak.columns) display(sorted_cols) <pre>\n---------------------------------------------------------------------------\nNameError                                 Traceback (most recent call last)\nCell In[2], line 1\n----&gt; 1 mainpeak = tam.get_mainpeak_params(show_plot=True, plot_type=\"mean\")\n      2 sorted_cols = sorted(mainpeak.columns)\n      3 display(sorted_cols)\n\nNameError: name 'tam' is not defined</pre> In\u00a0[3]: Copied! <pre>processparams = ProcessingParameters()\nprocessparams.slope_analysis.flank_fraction_start = 0.85\nprocessparams.slope_analysis.flank_fraction_end = 0.95\n\nmainpeak_custom = tam.get_mainpeak_params(\n    processparams=processparams,\n    show_plot=True,\n    plot_type=\"mean\",\n)\n</pre> processparams = ProcessingParameters() processparams.slope_analysis.flank_fraction_start = 0.85 processparams.slope_analysis.flank_fraction_end = 0.95  mainpeak_custom = tam.get_mainpeak_params(     processparams=processparams,     show_plot=True,     plot_type=\"mean\", )  <pre>\n---------------------------------------------------------------------------\nNameError                                 Traceback (most recent call last)\nCell In[3], line 1\n----&gt; 1 processparams = ProcessingParameters()\n      2 processparams.slope_analysis.flank_fraction_start = 0.85\n      3 processparams.slope_analysis.flank_fraction_end = 0.95\n      4 \n\nNameError: name 'ProcessingParameters' is not defined</pre> In\u00a0[4]: Copied! <pre>tam_flank = Measurement(\n    folder=datapath,\n    regex=r\".*flank_detection3.*\",\n    show_info=False,\n)\n\nparams_ascending = ProcessingParameters()\nparams_ascending.cutoff.cutoff_min = 60\nparams_ascending.spline_interpolation.apply = True\nparams_ascending.spline_interpolation.smoothing_1st_deriv = 5e-12\nparams_ascending.slope_analysis.flank_fraction_start = 0.2\nparams_ascending.slope_analysis.flank_fraction_end = 0.9\nparams_ascending.slope_analysis.first_ascending_fraction_of_max = 0.6\nparams_ascending.slope_analysis.first_ascending_range_method = \"delta\"\nparams_ascending.slope_analysis.first_ascending_delta_y_w_g = 1e-3\nparams_ascending.slope_analysis.flexible = 0.0\n\nmainpeak_ascending = tam_flank.get_mainpeak_params(\n    processparams=params_ascending,\n    show_plot=True,\n    method=\"ascending\",\n)\n</pre> tam_flank = Measurement(     folder=datapath,     regex=r\".*flank_detection3.*\",     show_info=False, )  params_ascending = ProcessingParameters() params_ascending.cutoff.cutoff_min = 60 params_ascending.spline_interpolation.apply = True params_ascending.spline_interpolation.smoothing_1st_deriv = 5e-12 params_ascending.slope_analysis.flank_fraction_start = 0.2 params_ascending.slope_analysis.flank_fraction_end = 0.9 params_ascending.slope_analysis.first_ascending_fraction_of_max = 0.6 params_ascending.slope_analysis.first_ascending_range_method = \"delta\" params_ascending.slope_analysis.first_ascending_delta_y_w_g = 1e-3 params_ascending.slope_analysis.flexible = 0.0  mainpeak_ascending = tam_flank.get_mainpeak_params(     processparams=params_ascending,     show_plot=True,     method=\"ascending\", ) <pre>\n---------------------------------------------------------------------------\nNameError                                 Traceback (most recent call last)\nCell In[4], line 1\n----&gt; 1 tam_flank = Measurement(\n      2     folder=datapath,\n      3     regex=r\".*flank_detection3.*\",\n      4     show_info=False,\n\nNameError: name 'Measurement' is not defined</pre> <p>The DataFrame returned in <code>\"ascending\"</code> mode contains a different set of columns from the default <code>\"mean\"</code> mode. The slope and the timestamps that bracket the detected ascending segment are reported alongside the threshold and fraction-of-max settings used to find them.</p> In\u00a0[5]: Copied! <pre>first_ascending_cols = [\n    \"sample_short\",\n    \"gradient_of_first_ascending_slope_to_fraction_of_max\",\n    \"onset_time_s_from_first_ascending_slope\",\n    \"first_ascending_slope_start_time_s\",\n    \"first_ascending_slope_end_time_s\",\n    \"normalized_heat_flow_w_g_threshold_for_first_ascending_slope\",\n]\navailable = [c for c in first_ascending_cols if c in mainpeak_ascending.columns]\nmainpeak_ascending[available]\n</pre> first_ascending_cols = [     \"sample_short\",     \"gradient_of_first_ascending_slope_to_fraction_of_max\",     \"onset_time_s_from_first_ascending_slope\",     \"first_ascending_slope_start_time_s\",     \"first_ascending_slope_end_time_s\",     \"normalized_heat_flow_w_g_threshold_for_first_ascending_slope\", ] available = [c for c in first_ascending_cols if c in mainpeak_ascending.columns] mainpeak_ascending[available] <pre>\n---------------------------------------------------------------------------\nNameError                                 Traceback (most recent call last)\nCell In[5], line 9\n      5     \"first_ascending_slope_start_time_s\",\n      6     \"first_ascending_slope_end_time_s\",\n      7     \"normalized_heat_flow_w_g_threshold_for_first_ascending_slope\",\n      8 ]\n----&gt; 9 available = [c for c in first_ascending_cols if c in mainpeak_ascending.columns]\n     10 mainpeak_ascending[available]\n\nNameError: name 'mainpeak_ascending' is not defined</pre>"},{"location":"example_get_mainpeak_params.html#extracting-main-peak-parameters","title":"Extracting Main-Peak Parameters\u00b6","text":"<p>In isothermal heat-flow calorimetry of cementitious materials, the main hydration peak (driven by the silicate reaction) is characterized by parameters such as the maximum slope, the mean slope of the ascending flank, and the times at which these features occur.</p> <p><code>Measurement.get_mainpeak_params()</code> extracts these parameters in a single call and returns them in a <code>pandas.DataFrame</code>. Two analysis modes are available:</p> <ul> <li><code>method=\"mean\"</code> (default) \u2014 combines a maximum-slope and a mean-slope (flank-tangent) analysis.</li> <li><code>method=\"ascending\"</code> \u2014 uses a first-ascending-slope-to-fraction approach, which can be more robust for noisy or asymmetric peaks.</li> </ul> <p>This tutorial walks through both, starting from a minimal call and progressively customizing the processing parameters.</p>"},{"location":"example_get_mainpeak_params.html#a-minimal-call","title":"A minimal call\u00b6","text":"<p>Without any custom processing parameters, <code>get_mainpeak_params()</code> runs both the maximum-slope and mean-slope analyses on the loaded sample. Setting <code>show_plot=True</code> and <code>plot_type=\"mean\"</code> produces a flank-tangent visualization alongside the returned DataFrame.</p>"},{"location":"example_get_mainpeak_params.html#customizing-the-processing","title":"Customizing the processing\u00b6","text":"<p>Real calorimetry data is rarely clean enough for the defaults to give the best result. Three groups of <code>ProcessingParameters</code> typically matter for slope analysis:</p> <ul> <li>Smoothing \u2014 <code>median_filter</code> removes single-point noise; <code>spline_interpolation</code> smooths the first and second derivatives, which the slope detection relies on.</li> <li>Cutoff \u2014 <code>cutoff.cutoff_min</code> discards the early measurement period (mixing artefacts, thermal equilibration) so they don't bias the analysis.</li> <li>Flank fraction \u2014 <code>slope_analysis.flank_fraction_start/_end</code> define which portion of the ascending flank counts as \"the slope\" for the mean-slope (flank-tangent) analysis.</li> </ul>"},{"location":"example_get_mainpeak_params.html#alternative-analysis-methodascending","title":"Alternative analysis: <code>method=\"ascending\"</code>\u00b6","text":"<p>For noisy or asymmetric peaks, where a symmetric flank fit struggles, the first-ascending-slope-to-fraction approach can be more robust. Instead of fitting a tangent to a defined fraction of the flank, the algorithm walks up the rising heat-flow signal until it reaches a target fraction of the peak maximum (or a target heat-flow delta) and returns the slope of that ascent.</p> <p>The next example switches to the <code>flank_detection3</code> dataset, which is well-suited to demonstrating this analysis, and uses parameters tuned for first-ascending detection.</p>"},{"location":"example_get_mainpeak_params.html#closing-notes","title":"Closing notes\u00b6","text":"<p>Inspect <code>mainpeak_custom.columns</code> or <code>mainpeak_ascending.columns</code> to see the full set of values returned for your data \u2014 many additional intermediate quantities are available alongside the slopes and timestamps.</p> <p>Further customization is available through <code>ProcessingParameters.plotting</code> (figure size, axis limits, legend position, plot titles drawn from metadata columns, monochrome styling) and through <code>save_plot=True</code> / <code>plotpath=...</code> for batch plot export. Sample-specific parameter overrides are supported via <code>processparams.add_sample_param_rule(regex, {...})</code>.</p> <p>Related methods for other aspects of the heat-flow signal:</p> <ul> <li><code>Measurement.get_peaks()</code> \u2014 peak detection across the full time series.</li> <li><code>Measurement.get_peak_onsets()</code> \u2014 gradient-threshold-based onset detection.</li> <li><code>Measurement.get_maximum_slope()</code> \u2014 maximum-slope detection only, without the combined output.</li> </ul>"},{"location":"how-to-guide.html","title":"How-To Guides","text":""},{"location":"how-to-guide.html#interfacing-with-experimental-results-file-from-tam-air-calorimeters-made-easy","title":"Interfacing with experimental results file from TAM Air calorimeters made easy.","text":"<p>After collecting multiple experimental results files from a TAM Air calorimeter you will be left with multiple .xls-files obtained as exports from the device control software. To achieve a side by side comparison of theses results and some basic extraction of relevant parameters, CaloCem is here to get this done smoothly.</p> <p>Note: CaloCem has been developed without involvement of TA Instruments and is thus independent from the company and its software.</p>"},{"location":"how-to-guide.html#info-downloads","title":"Info / Downloads","text":""},{"location":"how-to-guide.html#table-of-contents","title":"Table of Contents","text":"<ul> <li>Example Usage</li> <li>Basic plotting</li> <li>Getting cumulated heat values</li> <li>Identifying peaks</li> <li>Identifying peak onsets</li> <li>Plotting by Category</li> <li>Installation</li> <li>Contributing</li> </ul>"},{"location":"how-to-guide.html#example-usage","title":"Example Usage","text":"<p>Import <code>Measurement</code> from CaloCem.</p> <pre><code># import\nfrom pathlib import Path\nfrom calocem import Measurement\n</code></pre> <p>Next, we define where the exported files are stored. With this information at hand, a <code>Measurement</code> is initialized. Experimental raw data and the metadata passed in the course of the measurement are retrieved by the methods <code>get_data()</code> and <code>get_information()</code>, respectively.</p> <pre><code># define data path\n# \"mycalodata\" is the subfoldername where the calorimetry\n# data files (both .csv or .xlsx) are stored\n\npath_to_data = Path(__file__).parent / \"mycalodata\"\n\n# load experiments via class, i.e. instantiate Measurement object with data\ntam = Measurement(folder=path_to_data)\n\n# get sample and information\ndata = tam.get_data()\ninfo = tam.get_information()\n</code></pre>"},{"location":"how-to-guide.html#basic-plotting","title":"Basic plotting","text":"<p>Furthermore, the <code>Measurement</code> features a <code>plot()</code>-method for readily visualizing the collected results.</p> <pre><code># make plot\ntam.plot()\n# show plot\nplt.show()\n</code></pre> <p>Without further options specified, the <code>plot()</code>-method yields the following.</p> <p></p> <p>The <code>plot()</code>-method can also be tuned to show the temporal course of normalized heat. On the one hand, this \"tuning\" refers to the specification of further keyword arguments such as <code>t_unit</code> and <code>y</code>. On the other hand, the <code>plot()</code>-method returns an object of type <code>matplotlib.axes._subplots.AxesSubplot</code>, which can be used to further customize the plot. In the following, a guide-to-the-eye line is introduced next to adjuting the axes limts, which is not provided for via the <code>plot()</code>-method's signature.</p> <p><pre><code># show cumulated heat plot\nax = tam.plot(\n    t_unit=\"h\",\n    y='normalized_heat',\n    y_unit_milli=False\n)\n\n# define target time\ntarget_h = 1.5\n\n# guide to the eye line\nax.axvline(target_h, color=\"gray\", alpha=0.5, linestyle=\":\")\n\n# set upper limits\nax.set_ylim(top=250)\nax.set_xlim(right=6)\n# show plot\nplt.show()\n</code></pre> The following plot is obtained:</p> <p></p>"},{"location":"how-to-guide.html#getting-cumulated-heat-values","title":"Getting cumulated heat values","text":"<p>The cumulated heat after a certain period of time <code>target_h</code> from starting the measurement is a relevant quantity for answering different types of questions. For this purpose, the method <code>get_cumulated_heat_at_hours</code> returns an overview of this parameter for all the samples in the specified folder.</p> <pre><code># get table of cumulated heat at certain age\ncumulated_heats = tam.get_cumulated_heat_at_hours(\n          target_h=target_h,\n          cutoff_min=10\n          )\n\n# show result\nprint(cumulated_heats)\n</code></pre> <p>The return value of the method, <code>cumulated_heats</code> is a <code>pd.DataFrame</code>.</p>"},{"location":"how-to-guide.html#identifying-peaks","title":"Identifying peaks","text":"<p>Next to cumulated heat values detected after a certain time frame from starting the reaction, peaks characteristics can be obtained from the experimental data via the <code>get_peaks</code>-method.</p> <pre><code># get peaks\npeaks = tam.get_peaks(\n    show_plot=True,\n    prominence=0.00001,  # \"sensitivity of peak picking\"\n    cutoff_min=60,  # how much to discard at the beginning of the measurement\n    plt_right_s=4e5,\n    plt_top=1e-2,\n    regex=\".*_\\d\"  # filter samples\n    )\n</code></pre> <p>Tweaking some of the available keyword arguments, the following plot is obtained:</p> <p></p> <p>Please keep in mind, that in particular for samples of ordinary Portland cement (OPC) a clear and unambiguous identification/assigment of peaks remains a challenging task which cannot be achieved in each and every case by CaloCem. It is left to the user draw meaningful scientific conclusions from the characteristics derived from this method.</p>"},{"location":"how-to-guide.html#identifying-peak-onsets","title":"Identifying peak onsets","text":"<p>Similarly, the peak onset characteristics are accessible via the <code>get_peak_onsets</code>-method. The resulting plot is shown below.</p> <p><pre><code># get onsets\nonsets = tam.get_peak_onsets(\n    gradient_threshold=0.000001,\n    rolling=10,\n    exclude_discarded_time=True,\n    show_plot=True,\n    regex=\"OPC\"\n)\n</code></pre> </p>"},{"location":"how-to-guide.html#plotting-by-category","title":"Plotting by Category","text":"<p>For introducing the idea of plotting calorimetry data \"by category\" another set of experimental data will be introduced. Next to the calorimetry data alone, information on investigated samples is supplied via an additional source file. In the present example via the file <code>mini_metadata.csv</code>.</p> <p>To begin with, a <code>Measurement</code>-object is initialized for selected files from the specified <code>`path</code>.</p> <pre><code>from pathlib import Path\nfrom calocem import Measurement\n\n# path to experimental calorimetry files\npath = Path(__file__).parent / \"DATA\"\n\n# initialize Measurement object\ntam_II = Measurement(\n    path, regex=\"myexp.*\", show_info=True, cold_start=True, auto_clean=False\n)\n</code></pre> <p>Next, we need to connect the previously defined object to our metadata provided by the <code>mini_metadata.csv</code>-file. To establish this mapping between experimental results and metadata, the file location, i.e. path, and the column name containing the exact(!) names of the calorimetry files needs to be passed to the <code>add_metadata_source</code>-method. In our case, we declare the column <code>experiment_nr</code> for this purpose</p> <pre><code># add metadata\ntam.add_metadata_source(\"mini_metadata.csv\", \"experiment_nr\")\n</code></pre> <p>Finally, a plotting by category can be carried out by one or multiple categories as shown in the following.</p> <pre><code># define action by one category\ncategorize_by = \"cement_name\"  # 'date', 'cement_amount_g', 'water_amount_g'\n\n# # define action by two or more categories\ncategorize_by = [\"date\", \"cement_name\"]\n\n# loop through plots via generator\nfor this_plot in tam.plot_by_category(categorize_by):\n    # extract parts obtained from generator\n    category_value, ax = this_plot\n    # fine tuning of plot/cosmetics\n    ax.set_ylim(0, 3)\n    # show plot\n    plt.show()\n</code></pre> <p>This yields plots of the following kind.</p> <p></p> <p></p>"},{"location":"how-to-guide.html#installation","title":"Installation","text":"<p>Use the package manager pip to install CaloCem.</p> <pre><code>pip install CaloCem\n</code></pre>"},{"location":"how-to-guide.html#contributing","title":"Contributing","text":"<p>Pull requests are welcome. For major changes, please open an issue first to discuss what you would like to change.</p> <p>Please make sure to update tests as appropriate.</p> <p>List of contributors - mj-hofmann - tgaedt</p>"},{"location":"how-to-guide.html#license","title":"License","text":"<p>GNU GPLv3</p>"},{"location":"how-to-guide.html#test","title":"Test","text":""},{"location":"how-to-guide.html#code-styling","title":"Code Styling","text":""},{"location":"plotting.html","title":"Plotting","text":"<p>There are many different use cases for isothermal calorimetry. Here, we focus on the application of isothermal heat flow calorimetry for the hydration of cementitious materials.</p>"},{"location":"plotting.html#basic-plotting-of-calorimetry-data","title":"Basic Plotting of Calorimetry Data","text":"<p>Assume that your calorimetry data is found inside a folder called <code>calo_data</code> and your Python script <code>myscript.py</code>is in the working directory. <pre><code>.\n\u251c\u2500\u2500 myscript.py\n\u2514\u2500\u2500 calo_data\n    \u251c\u2500\u2500 calofile1.csv\n    \u2514\u2500\u2500 calofile2.csv\n</code></pre></p> <p>It is very easy to load the calorimetry files and to plot them. The file <code>myscript.py</code> could read like this. First, we create a Path object <code>datapath</code> using the pathlib package that is directed at the folder which contains the raw instrument data (<code>calo_data</code> in this example). The advantage of using the <code>pathlib</code> package is that we do not have to worry if the user of our code is running Linux, MacOS, or Windows. The <code>Path()</code> object ensures that the path definition always works. In our example, <code>Path(__file__).parent</code> contains the absolute path to the folder in which the script (here <code>myscript.py</code>) is located, independent of the operating system. By writing <code>Path(__file__).parent / \"calo_data\"</code> we create a <code>Path()</code> object which contains the absolute path to <code>calo_data</code>.</p> <p>After we have obtained the path, we pass it to <code>ta.Measurement()</code>. Besides the <code>Path</code> object, we can pass further arguments such as the option <code>show_info=True</code> which prints the names of the calo files being loaded in the terminal.</p> <p><pre><code>from pathlib import Path\nfrom calocem import Measurement\n\ndatapath = Path(__file__).parent / \"calo_data\"\n\n# create the calorimetry Measurement object\ntam = Measurement(\n    folder=datapath,\n    show_info=True,\n    auto_clean=False,\n)\n\n# plot the data\ntam.plot()\n</code></pre> This would yield something like the following plot:</p> <p></p> <p>The plot has at least three issues:</p> <ul> <li>the y-axis and the x-axis are automatically scaled to include the maximumum values</li> <li>the legend is not visible</li> <li>by default the plot method plots the normalized heat flow in mW/g, maybe another parameters is desired</li> </ul>"},{"location":"plotting.html#customizing-the-plot","title":"Customizing the plot","text":""},{"location":"plotting.html#choosing-different-variables-for-the-y-axis","title":"Choosing different variables for the y-axis","text":"<p>If only a different y-axis variable is desired, this can simply be achieved by defining the name of the desired parameter:</p> <pre><code>tam.plot(y=\"heat_j_g\")\n</code></pre>"},{"location":"plotting.html#full-customization","title":"Full customization","text":"<p>The <code>plot()</code> method returns a Matplotlib axes object.  Therefore, we can manipulate the plot as normal, e.g., by defining the limits of both axes or by defining the location of the legend (as shown in the code below).</p> <pre><code>ax = tam.plot(\n    y=\"normalized_heat_flow_w_g\",\n    t_unit=\"h\",  # time axis in hours\n    y_unit_milli=True,\n)\n\n# set upper limits\nax.set_ylim(0, 6)\nax.set_xlim(0, 48)\nax.legend(bbox_to_anchor=(1., 1), loc=\"upper right\")\n</code></pre>"},{"location":"plotting.html#plotting-heat-flow-and-heat-in-subplots","title":"Plotting Heat Flow and Heat in Subplots","text":"<p>Often, both the initial phase of hydration is of interest and also both the heat flow and the heat are relevant.  Here is code which allows plotting such data to a neat 2x2 grid.</p> <pre><code>plot_configs = [\n    {\"ycol\": \"normalized_heat_flow_w_g\", \"xlim\": 1, \"ylim\": 0.05},\n    {\"ycol\": \"normalized_heat_flow_w_g\", \"xlim\": 48, \"ylim\": 0.005},\n    {\"ycol\": \"normalized_heat_j_g\", \"xlim\": 1, \"ylim\": 30},\n    {\"ycol\": \"normalized_heat_j_g\", \"xlim\": 48, \"ylim\": 300},\n]\n\nimport matplotlib.pyplot as plt\n\nfig, axs = plt.subplots(2, 2, layout=\"constrained\")\nfor ax, config in zip(axs.flatten(), plot_configs):\n    tam.plot(y=config[\"ycol\"], t_unit=\"h\", y_unit_milli=False, ax=ax)\n    ax.set_xlim(0, config[\"xlim\"])\n    ax.set_ylim(0, config[\"ylim\"])\n    ax.get_legend().remove()\nplt.show()\n</code></pre> <p></p>"},{"location":"plotting_example.html","title":"Plotting Example","text":"In\u00a0[1]: Copied! <pre>import matplotlib.pyplot as plt\nfrom calocem.tacalorimetry import Measurement\n\nfrom pathlib import Path\n\n# here you need to define the suitable path to the data\n# this reads a little cryptic because it is a relative path from the docs folder of the repo\ndatapath = Path().cwd().parent / \"calocem\" / \"DATA\"\n\ntam = Measurement(datapath, regex=\".*bm2.csv\")\n\nfig, ax = plt.subplots()\ntam.plot(ax=ax)\nax.set_xlim(0, 30)\nax.set_ylim(0, 5)\nax.set_title(\"TACalorimetry Measurement\")\nplt.show()\n</pre> import matplotlib.pyplot as plt from calocem.tacalorimetry import Measurement  from pathlib import Path  # here you need to define the suitable path to the data # this reads a little cryptic because it is a relative path from the docs folder of the repo datapath = Path().cwd().parent / \"calocem\" / \"DATA\"  tam = Measurement(datapath, regex=\".*bm2.csv\")  fig, ax = plt.subplots() tam.plot(ax=ax) ax.set_xlim(0, 30) ax.set_ylim(0, 5) ax.set_title(\"TACalorimetry Measurement\") plt.show() <pre>\n---------------------------------------------------------------------------\nModuleNotFoundError                       Traceback (most recent call last)\nCell In[1], line 1\n----&gt; 1 import matplotlib.pyplot as plt\n      2 from calocem.tacalorimetry import Measurement\n      3 \n      4 from pathlib import Path\n\nModuleNotFoundError: No module named 'matplotlib'</pre> In\u00a0[2]: Copied! <pre>tam = Measurement(datapath, regex=\".*bm.*\")\n\nplot_configs = [\n    {\"ycol\": \"normalized_heat_flow_w_g\", \"xlim\": 1, \"ylim\": 0.050},\n    {\"ycol\": \"normalized_heat_flow_w_g\", \"xlim\": 48, \"ylim\": 0.005},\n    {\"ycol\": \"normalized_heat_j_g\", \"xlim\": 1, \"ylim\": 30},\n    {\"ycol\": \"normalized_heat_j_g\", \"xlim\": 48, \"ylim\": 300},\n]\n\nfig, axs = plt.subplots(2, 2, layout=\"constrained\")\nfor ax, config in zip(axs.flatten(), plot_configs):\n    tam.plot(y=config[\"ycol\"], t_unit=\"h\", y_unit_milli=False, ax=ax)\n    ax.set_xlim(0, config[\"xlim\"])\n    ax.set_ylim(0, config[\"ylim\"])\n    ax.get_legend().remove()\nplt.show()\n</pre>  tam = Measurement(datapath, regex=\".*bm.*\")  plot_configs = [     {\"ycol\": \"normalized_heat_flow_w_g\", \"xlim\": 1, \"ylim\": 0.050},     {\"ycol\": \"normalized_heat_flow_w_g\", \"xlim\": 48, \"ylim\": 0.005},     {\"ycol\": \"normalized_heat_j_g\", \"xlim\": 1, \"ylim\": 30},     {\"ycol\": \"normalized_heat_j_g\", \"xlim\": 48, \"ylim\": 300}, ]  fig, axs = plt.subplots(2, 2, layout=\"constrained\") for ax, config in zip(axs.flatten(), plot_configs):     tam.plot(y=config[\"ycol\"], t_unit=\"h\", y_unit_milli=False, ax=ax)     ax.set_xlim(0, config[\"xlim\"])     ax.set_ylim(0, config[\"ylim\"])     ax.get_legend().remove() plt.show() <pre>\n---------------------------------------------------------------------------\nNameError                                 Traceback (most recent call last)\nCell In[2], line 1\n----&gt; 1 tam = Measurement(datapath, regex=\".*bm.*\")\n      2 \n      3 plot_configs = [\n      4     {\"ycol\": \"normalized_heat_flow_w_g\", \"xlim\": 1, \"ylim\": 0.050},\n\nNameError: name 'Measurement' is not defined</pre> In\u00a0[\u00a0]: Copied! <pre>\n</pre>"},{"location":"plotting_example.html#plotting-example","title":"Plotting Example\u00b6","text":"<p>This is written as a Jupyter Notebook inside the  <code>docs</code> folder of the repository.</p> <p>The <code>Measurement</code> class loads the calorimetry data. You need to pass a folder name. Ideally a pathlib <code>Path</code> object containing the correct path is passed to <code>Measurement</code>. By default, the <code>Measurement</code> will load all data files contained in the folder that is passed. If you want to only load a specific file, you can pass a regex filter (as shown in the example below).</p> <p>The <code>Measurement</code> object has several methods. Here we use the <code>plot</code> method to create the heat flow plot. If you create an axis object before, the method will draw the plot onto the axis object if it is passed (see the example below). The axis object can then be modified with the methods of Matplotlib.</p>"},{"location":"plotting_example.html#folder-definition","title":"Folder Definition\u00b6","text":"<p>If your folder looks like this, and your script is <code>myscript.py</code></p> <pre>.\n\u251c\u2500\u2500 myscript.py\n\u2514\u2500\u2500 calo_data\n    \u251c\u2500\u2500 calofile1.csv\n    \u2514\u2500\u2500 calofile2.csv\n</pre> <p>then the pathlib definition in <code>myscript.py</code> would look like</p> <pre>from pathlib import Path\ndatapath = Path(__file__).parent / \"calo_data\"\n</pre> <p>Here in the example above, however, the Jupyter Notebook is located in a special location, therefore the path looks a bit cryptic.</p>"},{"location":"plotting_example.html#customizing-the-plot","title":"Customizing the plot\u00b6","text":"<p>Often we load more than one calorimetry experiment. In the following example, two files are loaded. Additionally, we want to plot the data such that we both see the very early heat development and the later heat development. We combine Matplotlib with CaloCem.</p>"},{"location":"quantification.html","title":"Quantification of Calorimetry Data","text":""},{"location":"quantification.html#peak-detection","title":"Peak Detection","text":"<p>First we load the data</p> <p><pre><code># %%\nfrom pathlib import Path\nimport matplotlib.pyplot as plt\nfrom calocem import Measurement, ProcessingParameters\n\n# define the Path of the folder with the calorimetry data\ndatapath = Path()\n\n# experiments via class\ntam = Measurement(\n    folder=datapath,\n    regex=r\".*file.*\",\n    show_info=True,\n    auto_clean=False,\n    cold_start=True,\n)\n</code></pre> Then, before we apply the <code>get_peaks()</code> method, we can define the ProcessingParameters if the default options are not suitable. Here we define the peak prominence and give it the value 1e-4. Note that only the largest peak will be detected if the prominence is set to 1e-3 in this example.</p> <p><pre><code># define the processing parameters\nprocessparams = ProcessingParameters()\nprocessparams.peakdetection.prominence = 1e-4\n\n# plot the peak position\nfig, ax = plt.subplots()\n\n# get peaks (returns both a dataframe and extends the axes object)\npeaks_found = tam.get_peaks(processparams, plt_right_s=3e5, ax=ax, show_plot=True)\nax.set_xlim(0, 100000)\nplt.show()\n</code></pre> </p> <p><code>peaks_found</code> is a tuple.  The first element contains the Dataframe with the parameters of the detected peaks. It could be exported to a csv file via</p> <pre><code>df = peaks_found[0].iloc[:,[0,5,6,9]]\ndf.to_csv(plotpath / \"example_get_peaks.csv\", index=False)\n</code></pre> <p>The dataframe looks like this:</p> time_s normalized_heat_flow_w_g normalized_heat_j_g prominence 29565.2 0.00207652 75.7002 0.00109303 82364.8 0.00164736 162.984 0.000207758"},{"location":"quantification.html#maximum-slope-of-c3s-reaction-detection","title":"Maximum Slope (of C3S Reaction) Detection","text":"<p>Programmatic, automatic detection the maximum slope can be a little tricky. The first example is straightforward and looks a normal Portland cement hydration case. Assuming that the data is already loaded, we can instantiate the ProcessingParameters object. The algorithm detects the maxima of the gradient of the heat flow. It is therefore very useful to apply a little smoothing to the first derivative.</p> <p><pre><code>processparams = ProcessingParameters()\nprocessparams.spline_interpolation.apply = True\nprocessparams.spline_interpolation.smoothing_1st_deriv = 1e-12\n\n# get peak onsets via alternative method\nfig, ax = plt.subplots()\nonsets_spline = tam.get_maximum_slope(\n    processparams=processparams,\n    show_plot=True,\n    ax = ax\n)\n</code></pre> If we set the parameter <code>show_plot</code> to <code>True</code>, we get a nice visual feedback on the gradient and the detected maximum slope (as a green line). The gradient is multiplied by a factor of 10.000 and shifted upwards.</p> <p></p>"},{"location":"reference.html","title":"Reference","text":"<p>This part of the project documentation shows the technical implementation  of the <code>CaloCem</code> project code.</p> <p>Refactored main measurement class for calorimetry data handling.</p>"},{"location":"reference.html#calocem.measurement.Measurement","title":"<code>Measurement</code>","text":"<p>Class for handling and processing isothermal heat flow calorimetry data.</p> <p>This class coordinates file I/O, data processing, analysis, and visualization operations while maintaining the same API as the original implementation.</p> Source code in <code>calocem/measurement.py</code> <pre><code>class Measurement:\n    \"\"\"\n    Class for handling and processing isothermal heat flow calorimetry data.\n\n    This class coordinates file I/O, data processing, analysis, and visualization\n    operations while maintaining the same API as the original implementation.\n    \"\"\"\n\n    def __init__(\n        self,\n        folder: Optional[Union[str, pathlib.Path]] = None,\n        show_info: bool = True,\n        regex: Optional[str] = None,\n        auto_clean: bool = False,\n        cold_start: bool = True,\n        processparams: Optional[ProcessingParameters] = None,\n        new_code: bool = False,\n        processed: bool = False,\n        metadata_path: Optional[Union[str, pathlib.Path]] = None,\n        metadata_id_column: Optional[str] = None,\n        save_cache: bool = False,\n    ):\n        \"\"\"\n        Initialize measurements from folder or existing data.\n\n        Parameters\n        ----------\n        folder : str or pathlib.Path, optional\n            Path to folder containing experimental files\n        show_info : bool, optional\n            Whether to print informative messages, by default True\n        regex : str, optional\n            Regex pattern to filter files, by default None\n        auto_clean : bool, optional\n            Whether to clean data automatically, by default False\n        cold_start : bool, optional\n            Whether to read from files or use cached data, by default True\n        processparams : ProcessingParameters, optional\n            Processing parameters, by default None. If None, the default parameters will be used\n        new_code : bool, optional\n            Flag for new code features, by default False\n        processed : bool, optional\n            Whether data is already processed, i.e., if a .csv file is used which was processed  by Calocem. By default False\n        metadata_path : str or pathlib.Path, optional\n            Path to metadata file (CSV, Excel, etc.), by default None\n        metadata_id_column : str, optional\n            Column name in metadata file that matches sample names, by default None\n        save_cache : bool, optional\n            Whether to write `_data.pickle` and `_info.pickle` cache files when loading\n            from a folder, by default False. When True, subsequent runs can be sped up\n            with ``cold_start=False`` to read from the cache instead of re-parsing the\n            folder. When False (default), no pickle files are created.\n        \"\"\"\n        # Initialize attributes\n        self._data = pd.DataFrame()\n        self._info = pd.DataFrame()\n        self._data_unprocessed = pd.DataFrame()\n        self._metadata = pd.DataFrame()\n        self._metadata_id = \"\"\n\n        # Store configuration\n        self._new_code = new_code\n        self._processed = processed\n        self._save_cache = save_cache\n\n        # Setup processing parameters\n        if not isinstance(processparams, ProcessingParameters):\n            self.processparams = ProcessingParameters()\n        else:\n            self.processparams = processparams\n\n        # Initialize components\n        self._folder_loader = FolderDataLoader(processed=processed)\n        self._data_persistence = DataPersistence()\n        self._data_cleaner = DataCleaner()\n        self._plotter = SimplePlotter()\n\n        # Load data if folder provided\n        if folder:\n            try:\n                if cold_start:\n                    self._load_from_folder(folder, regex, show_info)\n                else:\n                    self._load_from_cache()\n\n                if auto_clean:\n                    self._auto_clean_data()\n\n            except Exception as e:\n                if show_info:\n                    print(f\"Error during initialization: {e}\")\n                if auto_clean:\n                    raise AutoCleanException()\n                if not cold_start:\n                    raise ColdStartException()\n                raise\n\n        # Load metadata if provided\n        if metadata_path and metadata_id_column:\n            self.add_metadata_source(metadata_path, metadata_id_column, show_info)\n\n        # Apply downsampling if requested\n        if self.processparams.downsample.apply:\n            self._apply_adaptive_downsampling()\n\n        # Information message\n        if show_info:\n            print(\"================\")\n            print(\n                \"Are you missing some samples? Try rerunning with auto_clean=True and cold_start=True.\"\n            )\n            print(\"================\")\n\n    def _load_from_folder(\n        self, folder: Union[str, pathlib.Path], regex: Optional[str], show_info: bool\n    ):\n        \"\"\"Load data from folder using file loader.\"\"\"\n        try:\n            self._data, self._info = self._folder_loader.load_from_folder(\n                folder, regex, show_info\n            )\n            self._data_unprocessed = self._data.copy()\n\n            # Save to cache only if requested\n            if self._save_cache:\n                self._data_persistence.save_data(self._data, self._info)\n\n        except Exception as e:\n            raise DataProcessingException(\"load_from_folder\", e)\n\n    def _load_from_cache(self):\n        \"\"\"Load data from cached pickle files.\"\"\"\n        try:\n            if not self._data_persistence.pickle_files_exist():\n                raise FileNotFoundError(\"No pickle files found for cold start\")\n\n            self._data, self._info = self._data_persistence.load_data()\n            self._data_unprocessed = self._data.copy()\n\n        except Exception as e:\n            raise ColdStartException() from e\n\n    def _auto_clean_data(self):\n        \"\"\"Apply automatic data cleaning.\"\"\"\n        try:\n            self._data = self._data_cleaner.auto_clean_data(self._data)\n        except Exception as e:\n            raise AutoCleanException() from e\n\n    def _apply_adaptive_downsampling(self):\n        \"\"\"Apply adaptive downsampling if configured.\"\"\"\n        # TODO: Implement downsampling logic\n        logger.info(\"Downsampling requested but not yet implemented\")\n\n    # Data access methods\n    def get_data(self) -&gt; pd.DataFrame:\n        \"\"\"Get the processed calorimetry data.\"\"\"\n        return self._data\n\n    def get_information(self) -&gt; pd.DataFrame:\n        \"\"\"Get the measurement information/metadata.\"\"\"\n        return self._info\n\n    def get_metadata(self) -&gt; tuple:\n        \"\"\"Get added metadata and the ID column name.\"\"\"\n        return self._metadata, self._metadata_id\n\n    def get_sample_names(self) -&gt; list:\n        \"\"\"Get list of sample names.\"\"\"\n        return [\n            pathlib.Path(str(sample)).stem\n            for sample, _ in SampleIterator.iter_samples(self._data)\n        ]\n\n    # Plotting methods\n    def plot(\n        self,\n        t_unit: str = \"h\",\n        y: str = \"normalized_heat_flow_w_g\",\n        y_unit_milli: bool = True,\n        regex: Optional[str] = None,\n        show_info: bool = True,\n        ax=None,\n    ):\n        \"\"\"Plot the calorimetry data.\"\"\"\n        return self._plotter.plot_data(\n            self._data, t_unit, y, y_unit_milli, regex, show_info, ax\n        )\n\n    def plot_heatflow_with_gradient(\n        self,\n        processparams: Optional[ProcessingParameters] = None,\n        t_unit: str = \"h\",\n        target_col: str = \"normalized_heat_flow_w_g\",\n        age_col: str = \"time_s\",\n        y_unit_milli: bool = True,\n        gradient_unit_milli: bool = True,\n        gradient_color: Optional[str] = \"orange\",\n        align_zeros: bool = True,\n        show_zero_line: bool = False,\n        grid: bool = False,\n        regex: Optional[str] = None,\n        show_info: bool = True,\n        ax=None,\n    ):\n        \"\"\"Plot heat flow curve(s) with their gradient on a secondary y-axis.\n\n        The gradient is computed using the smoothing settings (rolling mean,\n        median filter, spline interpolation) from ``processparams`` (or the\n        instance's processparams when not provided).\n\n        Returns\n        -------\n        (ax, ax_grad) : tuple of matplotlib.axes.Axes\n            Primary (heat flow) and secondary (gradient) axes.\n        \"\"\"\n        params = processparams or self.processparams\n        return self._plotter.plot_heatflow_with_gradient(\n            self._data,\n            params,\n            t_unit=t_unit,\n            target_col=target_col,\n            age_col=age_col,\n            y_unit_milli=y_unit_milli,\n            gradient_unit_milli=gradient_unit_milli,\n            gradient_color=gradient_color,\n            align_zeros=align_zeros,\n            show_zero_line=show_zero_line,\n            grid=grid,\n            regex=regex,\n            show_info=show_info,\n            ax=ax,\n        )\n\n    def plot_by_category(\n        self,\n        categories: str,\n        t_unit: str = \"h\",\n        y: str = \"normalized_heat_flow_w_g\",\n        y_unit_milli: bool = True,\n    ):\n        \"\"\"Plot data by metadata categories.\"\"\"\n        # Simplified implementation - would need full metadata integration\n        logger.warning(\n            \"plot_by_category requires metadata integration - not fully implemented\"\n        )\n        yield from []\n\n    # Analysis methods\n    def get_peaks(\n        self,\n        processparams: Optional[ProcessingParameters] = None,\n        target_col: str = \"normalized_heat_flow_w_g\",\n        regex: Optional[str] = None,\n        cutoff_min: Optional[float] = None,  # Deprecated parameter\n        show_plot: bool = True,\n        plt_right_s: float = 2e5,\n        plt_top: float = 1e-2,\n        ax=None,\n        xunit: str = \"s\",\n        plot_labels: Optional[bool] = None,\n        xmarker: bool = False,\n    ) -&gt; pd.DataFrame:\n        \"\"\"Get DataFrame of peak characteristics.\"\"\"\n        if cutoff_min is not None:\n            warnings.warn(\n                \"The cutoff_min parameter is deprecated. Use ProcessingParameters instead.\",\n                DeprecationWarning,\n                stacklevel=2,\n            )\n\n        params = processparams or self.processparams\n        analyzer = PeakAnalyzer(params)\n        peaks_df = analyzer.get_peaks(self._data, target_col, regex)\n\n        if show_plot and not peaks_df.empty:\n            # Simple plotting implementation\n            for sample, sample_data in SampleIterator.iter_samples(self._data, regex):\n                sample_peaks = peaks_df[\n                    peaks_df[\"sample_short\"] == pathlib.Path(str(sample)).stem\n                ]\n                if not sample_peaks.empty:\n                    # Locate peak positions within sample_data by matching time_s values\n                    peak_indices = np.array([\n                        sample_data.index.get_loc(\n                            sample_data[\"time_s\"].sub(t).abs().idxmin()\n                        )\n                        for t in sample_peaks[\"time_s\"]\n                    ])\n                    self._plotter.plot_peaks(\n                        sample_data.reset_index(drop=True),\n                        peak_indices,\n                        str(sample),\n                        ax,\n                        \"time_s\",\n                        target_col,\n                    )\n\n        return peaks_df\n\n    def get_peak_onsets(\n        self,\n        target_col: str = \"normalized_heat_flow_w_g\",\n        age_col: str = \"time_s\",\n        time_discarded_s: float = 900,\n        rolling: int = 1,\n        gradient_threshold: float = 0.0005,\n        show_plot: bool = False,\n        exclude_discarded_time: bool = False,\n        regex: Optional[str] = None,\n        ax=None,\n    ):\n        \"\"\"Get peak onsets based on gradient threshold.\"\"\"\n        analyzer = OnsetAnalyzer(self.processparams)\n        return analyzer.get_peak_onsets(\n            self._data,\n            target_col,\n            age_col,\n            time_discarded_s,\n            rolling,\n            gradient_threshold,\n            exclude_discarded_time,\n            regex,\n        )\n\n    def get_deconvolution(\n        self,\n        processparams: Optional[ProcessingParameters] = None,\n        target_col: str = \"normalized_heat_flow_w_g\",\n        age_col: str = \"time_s\",\n        regex: Optional[str] = None,\n        n_peaks: Optional[int] = None,\n        peak_shape: str = \"lognormal\",\n        baseline_mode: Optional[str] = None,\n        relative_intensity_upper_bounds: Optional[list[float]] = None,\n        peak_width_upper_bounds: Optional[list[float]] = None,\n        show_plot: bool = False,\n        ax=None,\n    ) -&gt; pd.DataFrame:\n        \"\"\"Fit a multi-peak deconvolution model to each sample.\"\"\"\n        params = processparams or self.processparams\n        analyzer = DeconvolutionAnalyzer(params)\n        result = analyzer.get_deconvolution(\n            self._data,\n            target_col=target_col,\n            age_col=age_col,\n            regex=regex,\n            n_peaks=n_peaks,\n            peak_shape=peak_shape,\n            baseline_mode=baseline_mode,\n            relative_intensity_upper_bounds=relative_intensity_upper_bounds,\n            peak_width_upper_bounds=peak_width_upper_bounds,\n        )\n\n        if show_plot and not result.empty:\n            for sample, sample_data in SampleIterator.iter_samples(self._data, regex):\n                sample_short = pathlib.Path(str(sample)).stem\n                sample_result = result[result[\"sample_short\"] == sample_short]\n                if sample_result.empty:\n                    continue\n\n                plot_data = sample_data[[age_col, target_col]].copy()\n                if params.cutoff.cutoff_min:\n                    plot_data = plot_data[\n                        plot_data[age_col] &gt;= params.cutoff.cutoff_min * 60\n                    ]\n                plot_data = plot_data.replace([np.inf, -np.inf], np.nan).dropna()\n                if plot_data.empty:\n                    continue\n\n                x = plot_data[age_col].to_numpy(dtype=float)\n                y = plot_data[target_col].to_numpy(dtype=float)\n                x_logn = np.clip(x, 1e-12, None)\n                x_range = max(float(np.max(x) - np.min(x)), 1e-12)\n                x_scaled = 2.0 * (x - float(np.min(x))) / x_range - 1.0\n\n                created_ax = ax is None\n                if created_ax:\n                    _, local_ax = plt.subplots(figsize=(7, 5))\n                else:\n                    local_ax = ax\n\n                local_ax.plot(x, y, color=\"black\", linewidth=1.2, label=\"data\")\n\n                total_components = np.zeros_like(y)\n                shape = str(sample_result.iloc[0][\"peak_shape\"]).lower()\n                baseline = str(sample_result.iloc[0][\"baseline_mode\"]).lower()\n\n                for _, comp in sample_result.iterrows():\n                    amplitude = float(comp[\"amplitude\"])\n                    center = float(comp[\"center_time_s\"])\n                    width = float(comp[\"width\"])\n\n                    if shape == \"gaussian\":\n                        curve = analyzer._gaussian_peak(x, amplitude, center, width)\n                    else:\n                        curve = analyzer._lognormal_peak(x_logn, amplitude, center, width)\n\n                    total_components += curve\n                    local_ax.plot(\n                        x,\n                        curve,\n                        linestyle=\"--\",\n                        linewidth=1,\n                        label=f\"component {int(comp['component'])}\",\n                    )\n\n                baseline_constant = float(sample_result.iloc[0][\"baseline_constant\"])\n                baseline_slope = float(sample_result.iloc[0][\"baseline_slope\"])\n                if baseline == \"constant\" and not np.isnan(baseline_constant):\n                    total_fit = total_components + baseline_constant\n                elif baseline == \"linear\" and not np.isnan(baseline_constant):\n                    slope = 0.0 if np.isnan(baseline_slope) else baseline_slope\n                    total_fit = total_components + baseline_constant + slope * x\n                elif baseline == \"chebyshev\":\n                    cheb_coeffs = sample_result.iloc[0].get(\"baseline_cheb_coeffs\", None)\n                    if isinstance(cheb_coeffs, str):\n                        import ast\n\n                        cheb_coeffs = ast.literal_eval(cheb_coeffs)\n                    if cheb_coeffs is not None:\n                        baseline_curve = np.polynomial.chebyshev.chebval(\n                            x_scaled, np.array(cheb_coeffs, dtype=float)\n                        )\n                        total_fit = total_components + baseline_curve\n                    else:\n                        total_fit = total_components\n                else:\n                    total_fit = total_components\n\n                fit_r2 = sample_result[\"fit_r2\"].iloc[0]\n                local_ax.plot(\n                    x,\n                    total_fit,\n                    color=\"tab:red\",\n                    linewidth=1.5,\n                    label=f\"fit (R\u00b2={fit_r2:.3f})\" if not pd.isna(fit_r2) else \"fit\",\n                )\n                local_ax.set_title(f\"Deconvolution: {sample_short}\")\n                local_ax.set_xlabel(age_col)\n                local_ax.set_ylabel(target_col)\n                local_ax.legend()\n\n                if created_ax:\n                    plt.show()\n\n        return result\n\n    def get_left_peak_inflection_tangent_intersection(\n        self,\n        processparams: Optional[ProcessingParameters] = None,\n        target_col: str = \"normalized_heat_flow_w_g\",\n        age_col: str = \"time_s\",\n        regex: Optional[str] = None,\n        n_peaks: Optional[int] = None,\n        peak_shape: str = \"lognormal\",\n        baseline_mode: Optional[str] = None,\n        deconvolution_results: Optional[pd.DataFrame] = None,\n    ) -&gt; pd.DataFrame:\n        \"\"\"Determine abscissa intersection from tangent at left-peak flank inflection.\"\"\"\n        params = processparams or self.processparams\n        analyzer = DeconvolutionAnalyzer(params)\n\n        fit_results = deconvolution_results\n        if fit_results is None:\n            fit_results = self.get_deconvolution(\n                processparams=params,\n                target_col=target_col,\n                age_col=age_col,\n                regex=regex,\n                n_peaks=n_peaks,\n                peak_shape=peak_shape,\n                baseline_mode=baseline_mode,\n                show_plot=False,\n            )\n\n        return analyzer.get_left_peak_inflection_tangent_intersection(\n            self._data,\n            fit_results,\n            target_col=target_col,\n            age_col=age_col,\n            regex=regex,\n        )\n\n    def get_maximum_slope(\n        self,\n        processparams: Optional[ProcessingParameters] = None,\n        target_col: str = \"normalized_heat_flow_w_g\",\n        age_col: str = \"time_s\",\n        show_plot: bool = False,\n        regex: Optional[str] = None,\n        ax=None,\n        save_path: Optional[pathlib.Path] = None,\n        xunit: str = \"s\",\n    ) -&gt; pd.DataFrame:\n        \"\"\"Find the point in time of the maximum slope.\n\n        Wrapper around :meth:`get_mainpeak_params` returning only the\n        ``max_slope`` columns together with sample identifiers.\n        \"\"\"\n        result = self.get_mainpeak_params(\n            processparams=processparams,\n            target_col=target_col,\n            age_col=age_col,\n            show_plot=show_plot,\n            plot_type=\"max\",\n            regex=regex,\n            ax=ax,\n        )\n        if result.empty:\n            return result\n        id_cols = [\"sample\", \"sample_short\"]\n        max_slope_cols = [c for c in result.columns if \"max_slope\" in c]\n        return result[id_cols + max_slope_cols].reset_index(drop=True)\n\n    def get_mainpeak_params(\n        self,\n        processparams: Optional[ProcessingParameters] = None,\n        target_col: str = \"normalized_heat_flow_w_g\",\n        age_col: str = \"time_s\",\n        show_plot: bool = False,\n        save_plot: bool = False,\n        plot_type: str = \"mean\",\n        regex: Optional[str] = None,\n        plotpath: Optional[pathlib.Path] = None,\n        ax=None,\n        method: str = \"mean\",\n        monochrome: bool = False,\n        marker_size: float = 1.0,\n    ) -&gt; pd.DataFrame:\n        \"\"\"\n                Unified method for main-peak slope analysis.\n\n                Depending on ``method`` this either:\n                - ``\"mean\"`` (default): calculates maximum and mean slope onset analyses\n                - ``\"ascending\"``: calculates first ascending slope analysis via\n                    ``get_first_ascending_slope_to_fraction``\n\n                Results are returned in a single DataFrame with all available slope values.\n\n        Parameters\n        ----------\n        processparams : ProcessingParameters, optional\n            Processing parameters, by default None\n        target_col : str\n            Column containing heat flow data. The default is 'normalized_heat_flow_w_g'.\n        age_col : str\n            Column containing time data. The default is 'time_s'.\n        show_plot : bool\n            Whether to plot the results\n        plot_type : str\n            Type of plot to show: 'max', 'mean',\n            - 'max': Shows only maximum slope analysis plot\n            - 'mean': Shows only mean slope (flank tangent) analysis plot\n        regex : str, optional\n            Regex to filter samples\n        plotpath : pathlib.Path, optional\n            Path to save plots\n        ax : matplotlib.axes.Axes, optional\n            Matplotlib axes to plot on\n        method : str\n            Slope analysis method to run: 'mean' or 'ascending'.\n        monochrome : bool\n            If True, render all scatter/marker symbols on the analysis plot in\n            black instead of their default colors. Default is False.\n        marker_size : float\n            Multiplicative scale factor applied to all scatter/marker sizes on\n            the analysis plot. Default is 1.0.\n\n        Returns\n        -------\n        pd.DataFrame\n            Comprehensive DataFrame with available slope and characteristic results.\n\n        Examples\n        --------\n        &gt;&gt;&gt; measurement = Measurement(folder=\"data/\")\n        &gt;&gt;&gt; mainpeak_params = measurement.get_mainpeak_params(\n        ...     processparams=ProcessingParameters(),\n        ...     show_plot=False,\n        ...     plot_type=\"mean\",\n        ...     method=\"mean\",\n        ... )\n        \"\"\"\n        params = processparams or self.processparams\n\n        valid_methods = [\"mean\", \"ascending\"]\n        if method not in valid_methods:\n            raise ValueError(f\"method must be one of {valid_methods}, got '{method}'\")\n\n        if method == \"mean\":\n            max_slope_results = self._calculate_max_slope_analysis(\n                params,\n                target_col,\n                age_col,\n                regex,\n            )\n\n            mean_slope_results = self._calculate_mean_slope_analysis(\n                params,\n                target_col,\n                age_col,\n                regex,\n            )\n        elif method == \"ascending\":\n            max_slope_results = pd.DataFrame()\n            mean_slope_results = self._calculate_first_ascending_slope_analysis(\n                params,\n                target_col,\n                age_col,\n                regex,\n            )\n\n        dormant_minimum_heatflow = self.get_dormant_period_heatflow(\n            params, regex, show_plot=False\n        )\n\n        astm_values = self.get_astm_c1679_characteristics(params, individual=True, show_plot=False, regex=regex)\n\n        # Merge results into comprehensive DataFrame\n        combined_results = self._merge_slope_results(\n            max_slope_results, mean_slope_results, dormant_minimum_heatflow, astm_values\n        )\n\n        # Plot if requested\n        if (\n            method == \"mean\"\n            and (show_plot or save_plot)\n            and not (mean_slope_results.empty or max_slope_results.empty)\n        ):\n            self._plot_combined_slope_analysis(\n                combined_results,\n                params,\n                target_col,\n                age_col,\n                plot_type,\n                regex,\n                plotpath if save_plot else None,\n                ax,\n                show_plot=show_plot,\n                monochrome=monochrome,\n                marker_size=marker_size,\n            )\n            # if not ax:\n            #     plt.show()\n                # if (save_plot and show_plot) and plotpath:\n                #     plt.savefig(plotpath)\n                #     plt.show()\n                # elif show_plot:\n                #     plt.show()\n                # elif save_plot:\n                #     plt.savefig(plotpath)\n\n        elif (\n            method == \"ascending\"\n            and (show_plot or save_plot)\n            and not mean_slope_results.empty\n        ):\n            self._plot_ascending_slope_analysis(\n                combined_results,\n                params,\n                target_col,\n                age_col,\n                regex,\n                plotpath if save_plot else None,\n                ax,\n                show_plot=show_plot,\n                monochrome=monochrome,\n                marker_size=marker_size,\n            )\n\n        elif method == \"mean\" and mean_slope_results.empty:\n            # logger.warning(\"No slope analysis results to plot.\")\n            print(\"No mean slope analysis obtained - check the processing parameters.\")\n\n        elif method == \"mean\" and max_slope_results.empty:\n            print(\n                \"No maximum slope analysis obtained - check the processing parameters.\"\n            )\n\n        return combined_results\n\n    def _calculate_first_ascending_slope_analysis(\n        self,\n        params: ProcessingParameters,\n        target_col: str,\n        age_col: str,\n        regex: Optional[str],\n    ) -&gt; pd.DataFrame:\n        \"\"\"Calculate first ascending slope analysis and return structured results.\"\"\"\n        analyzer = FirstAscendingSlopeAnalyzer(params)\n        first_ascending_results = analyzer.get_first_ascending_slope_to_fraction(\n            self._data,\n            target_col=target_col,\n            age_col=age_col,\n            fraction_of_max=params.slope_analysis.first_ascending_fraction_of_max,\n            regex=regex,\n        )\n\n        if first_ascending_results.empty:\n            logger.warning(\"No first ascending slope results found.\")\n            return pd.DataFrame()\n\n        results = []\n        for _, row in first_ascending_results.iterrows():\n            sample = row.get(\"sample\", row.get(\"sample_short\", \"\"))\n            sample_short = row.get(\"sample_short\", row.get(\"sample\", \"\"))\n\n            representative_slope = row.get(\"first_ascending_slope\")\n            tangent_intercept = row.get(\"first_ascending_intercept\")\n            tangent_time_s = row.get(\"first_ascending_tangent_time_s\")\n\n            x_intersection = np.nan\n            x_intersection_dormant = np.nan\n            x_intersection_j_g = np.nan\n            x_intersection_dormant_j_g = np.nan\n            min_value_before_tangent = np.nan\n\n            sample_data = self._get_filtered_sample_data(\n                sample_short,\n                age_col,\n                cutoff_time_min=params.cutoff.cutoff_min,\n            )\n\n            peak_time_s = np.nan\n            peak_value = np.nan\n            peak_j_g = np.nan\n            if not sample_data.empty and target_col in sample_data.columns:\n                peak_idx = sample_data[target_col].idxmax()\n                if pd.notna(peak_idx):\n                    peak_row = sample_data.loc[peak_idx]\n                    peak_time_s = float(peak_row[age_col])\n                    peak_value = float(peak_row[target_col])\n                    if \"normalized_heat_j_g\" in sample_data.columns:\n                        peak_j_g = float(peak_row[\"normalized_heat_j_g\"])\n\n            if (\n                not sample_data.empty\n                and pd.notna(representative_slope)\n                and pd.notna(tangent_intercept)\n                and representative_slope != 0\n            ):\n                x_intersection = float(-tangent_intercept / representative_slope)\n\n                if pd.notna(tangent_time_s):\n                    data_before_tangent = sample_data[\n                        sample_data[age_col] &lt;= tangent_time_s\n                    ]\n                    if len(data_before_tangent) &gt; 0:\n                        min_value_before_tangent = float(\n                            data_before_tangent[target_col].min()\n                        )\n                        x_intersection_dormant = float(\n                            (min_value_before_tangent - tangent_intercept)\n                            / representative_slope\n                        )\n\n                x_values = sample_data[age_col].to_numpy(dtype=float)\n                j_values = sample_data[\"normalized_heat_j_g\"].to_numpy(dtype=float)\n                if np.isfinite(x_intersection):\n                    x_intersection_j_g = float(np.interp(x_intersection, x_values, j_values))\n                if np.isfinite(x_intersection_dormant):\n                    x_intersection_dormant_j_g = float(\n                        np.interp(x_intersection_dormant, x_values, j_values)\n                    )\n\n            onset_time = (\n                x_intersection_dormant\n                if np.isfinite(x_intersection_dormant)\n                else (\n                    x_intersection if np.isfinite(x_intersection) else tangent_time_s\n                )\n            )\n\n            result_data = {\n                \"sample\": sample,\n                \"sample_short\": sample_short,\n                \"fraction_of_max_for_first_ascending_slope\": row.get(\"fraction_of_max\"),\n                \"range_method_for_first_ascending_slope\": row.get(\n                    \"first_ascending_range_method\"\n                ),\n                \"delta_y_w_g_for_first_ascending_slope\": row.get(\n                    \"first_ascending_delta_y_w_g\"\n                ),\n                \"flexible_for_first_ascending_slope\": row.get(\n                    \"first_ascending_flexible\"\n                ),\n                \"delta_y_multiplier_for_first_ascending_slope\": row.get(\n                    \"first_ascending_delta_y_multiplier\"\n                ),\n                \"delta_y_effective_w_g_for_first_ascending_slope\": row.get(\n                    \"first_ascending_delta_y_effective_w_g\"\n                ),\n                \"normalized_heat_flow_w_g_threshold_for_first_ascending_slope\": row.get(\n                    \"fraction_threshold_value\"\n                ),\n                \"threshold_basis_for_first_ascending_slope\": row.get(\n                    \"fraction_threshold_basis\"\n                ),\n                \"gradient_of_first_ascending_slope_to_fraction_of_max\": row.get(\n                    \"first_ascending_slope\"\n                ),\n                \"first_ascending_mean_slope_time_s\": row.get(\n                    \"first_ascending_tangent_time_s\"\n                ),\n                \"normalized_heat_flow_w_g_at_first_ascending_mean_slope\": row.get(\n                    \"first_ascending_tangent_value\"\n                ),\n                \"first_ascending_slope_start_time_s\": row.get(\n                    \"first_ascending_start_time_s\"\n                ),\n                \"first_ascending_slope_end_time_s\": row.get(\"first_ascending_end_time_s\"),\n                \"normalized_heat_flow_w_g_at_first_ascending_slope_start\": row.get(\n                    \"first_ascending_start_value\"\n                ),\n                \"normalized_heat_flow_w_g_at_first_ascending_slope_end\": row.get(\n                    \"first_ascending_end_value\"\n                ),\n                \"number_of_points_for_first_ascending_slope\": row.get(\n                    \"first_ascending_n_points\"\n                ),\n                \"number_of_windows_for_first_ascending_mean_slope\": row.get(\n                    \"first_ascending_n_windows\"\n                ),\n                \"standard_deviation_for_first_ascending_mean_slope\": row.get(\n                    \"first_ascending_slope_std\"\n                ),\n                \"fraction_start_for_first_ascending_mean_slope\": row.get(\n                    \"first_ascending_fraction_start\"\n                ),\n                \"fraction_end_for_first_ascending_mean_slope\": row.get(\n                    \"first_ascending_fraction_end\"\n                ),\n                \"window_size_for_first_ascending_mean_slope\": row.get(\n                    \"first_ascending_window_size\"\n                ),\n                \"first_ascending_window_start_time_s\": row.get(\n                    \"first_ascending_window_start_time_s\"\n                ),\n                \"first_ascending_window_end_time_s\": row.get(\n                    \"first_ascending_window_end_time_s\"\n                ),\n                \"first_ascending_window_start_value\": row.get(\n                    \"first_ascending_window_start_value\"\n                ),\n                \"first_ascending_window_end_value\": row.get(\n                    \"first_ascending_window_end_value\"\n                ),\n                \"onset_time_s_from_first_ascending_slope\": onset_time,\n                \"onset_time_min_from_first_ascending_slope\": (\n                    onset_time / 60 if pd.notna(onset_time) else None\n                ),\n                \"onset_time_s_from_first_ascending_slope_abscissa\": (\n                    x_intersection if np.isfinite(x_intersection) else None\n                ),\n                \"normalized_heat_at_onset_time_first_ascending_slope_abscissa_j_g\": (\n                    x_intersection_j_g if np.isfinite(x_intersection_j_g) else None\n                ),\n                \"normalized_heat_at_onset_time_first_ascending_slope_dormant_j_g\": (\n                    x_intersection_dormant_j_g\n                    if np.isfinite(x_intersection_dormant_j_g)\n                    else None\n                ),\n                \"min_value_before_first_ascending_tangent\": (\n                    min_value_before_tangent if np.isfinite(min_value_before_tangent) else None\n                ),\n                \"peak_time_s\": peak_time_s if np.isfinite(peak_time_s) else None,\n                \"normalized_heat_flow_w_g_at_peak\": (\n                    peak_value if np.isfinite(peak_value) else None\n                ),\n                \"normalized_heat_j_g_at_peak\": peak_j_g if np.isfinite(peak_j_g) else None,\n            }\n            results.append(result_data)\n\n        return pd.DataFrame(results)\n\n    def _calculate_max_slope_analysis(\n        self,\n        params: ProcessingParameters,\n        target_col: str,\n        age_col: str,\n        regex: Optional[str],\n    ) -&gt; pd.DataFrame:\n        \"\"\"Calculate maximum slope analysis and return structured results.\"\"\"\n        # Get required data\n        max_slope_analyzer = SlopeAnalyzer(params)\n        max_slopes = max_slope_analyzer.get_maximum_slope(\n            self._data,\n            target_col,\n            age_col,\n            regex,\n        )\n\n        if max_slopes.empty:\n            logger.warning(\"No maximum slopes found. Check processing parameters.\")\n            return pd.DataFrame()\n\n        dormant_hfs = self.get_dormant_period_heatflow(params, regex, show_plot=False)\n        if dormant_hfs.empty:\n            logger.warning(\"No dormant period heat flows found.\")\n            return pd.DataFrame()\n\n        # Calculate onsets\n        analyzer = OnsetAnalyzer(params)\n        onsets = analyzer.get_peak_onset_via_max_slope(\n            self._data,\n            max_slopes,\n            dormant_hfs,  # intersection, xunit\n        )\n\n        # Structure results with consistent naming\n        results = []\n        for _, slope_row in max_slopes.iterrows():\n            sample = slope_row.get(\"sample\", slope_row.get(\"sample_short\", \"\"))\n            sample_short = slope_row.get(\"sample_short\", slope_row.get(\"sample\", \"\"))\n\n            onset_row = (\n                onsets[onsets[\"sample_short\"] == sample_short]\n                if not onsets.empty\n                else pd.DataFrame()\n            )\n            onset_time = (\n                onset_row.iloc[0][\"onset_time_s\"] if not onset_row.empty else None\n            )\n\n            # get normalized_heat_j_g at onset_time\n            if onset_time and not pd.isna(onset_time):\n                onset_j_g = np.interp(\n                    onset_time,\n                    self._data[age_col],\n                    self._data[\"normalized_heat_j_g\"],\n                )\n            else:\n                onset_j_g = None\n\n            result_data = {\n                \"sample\": sample,\n                \"sample_short\": sample_short,\n                \"gradient_from_max_slope\": slope_row.get(\"gradient\", 0),\n                \"curvature_at_max_slope\": slope_row.get(\"curvature\", 0),\n                \"max_slope_time_s\": slope_row.get(\"time_s\", 0),\n                \"normalized_heat_flow_w_g_at_max_slope\": slope_row.get(\n                    \"normalized_heat_flow_w_g\", 0\n                ),\n                \"normalized_heat_j_g_at_max_slope\": slope_row.get(\"normalized_heat_j_g\", 0),\n                \"normalized_heat_j_g_at_onset_time_max_slope\": onset_j_g,\n                \"onset_time_s_from_max_slope\": onset_time,\n                \"onset_time_min_max_slope\": onset_time / 60 if onset_time else None,\n                \"onset_time_s_max_slope_abscissa\": (\n                    onset_row.iloc[0][\"onset_time_s_abscissa\"]\n                    if not onset_row.empty\n                    else None\n                ),\n            }\n            results.append(result_data)\n\n        return pd.DataFrame(results)\n\n    def _calculate_mean_slope_analysis(\n        self,\n        params: ProcessingParameters,\n        target_col: str,\n        age_col: str,\n        regex: Optional[str],\n    ) -&gt; pd.DataFrame:\n        \"\"\"Calculate mean slope (flank tangent) analysis and return structured results.\"\"\"\n        analyzer = FlankTangentAnalyzer(params)\n\n        # Get flank tangent results\n        tangent_results = analyzer.get_ascending_flank_tangent(\n            self._data,\n            target_col,\n            age_col,\n            regex,\n        )\n\n        if tangent_results.empty:\n            logger.warning(\"No flank tangent results found.\")\n            return pd.DataFrame()\n\n        results = []\n        for _, row in tangent_results.iterrows():\n            sample = row.get(\"sample\", row.get(\"sample_short\", \"\"))\n            sample_short = row.get(\"sample_short\", row.get(\"sample\", \"\"))\n\n            # onset by intersection with tangent to dormant period\n            onset_time = row.get(\"x_intersection_dormant\", row.get(\"tangent_time_s\", 0))\n\n            result_data = {\n                \"sample\": sample,\n                \"sample_short\": sample_short,\n                \"gradient_of_mean_slope\": row.get(\"tangent_slope\", 0),\n                \"mean_slope_time_s\": row.get(\"tangent_time_s\", 0),\n                \"normalized_heat_flow_w_g_at_mean_slope\": row.get(\"tangent_value\", 0),\n                \"normalized_heat_j_g_at_mean_slope\": row.get(\"tangent_j_g\", 0),\n                \"onset_time_s_from_mean_slope\": onset_time,\n                \"onset_time_min_from_mean_slope\": onset_time / 60 if onset_time else None,\n                \"onset_time_s_from_mean_slope_abscissa\": row.get(\"x_intersection\", 0),\n                \"normalized_heat_at_onset_time_mean_slope_abscissa_j_g\": row.get(\"x_intersection_j_g\", 0),\n                \"normalized_heat_at_onset_time_mean_slope_dormant_j_g\": row.get(\"x_intersection_dormant_j_g\", 0),\n                \"flank_start_value\": row.get(\"flank_start_value\", 0),\n                \"flank_end_value\": row.get(\"flank_end_value\", 0),\n                \"peak_time_s\": row.get(\"peak_time_s\", 0),\n                \"normalized_heat_flow_w_g_at_peak\": row.get(\"peak_value\", 0),\n                \"normalized_heat_j_g_at_peak\": row.get(\"peak_j_g\", 0),\n            }\n            results.append(result_data)\n\n        return pd.DataFrame(results)\n\n    def _merge_slope_results(\n        self,\n        max_slope_results: pd.DataFrame,\n        mean_slope_results: pd.DataFrame,\n        dormant_hf_results: pd.DataFrame,\n        astm_results: pd.DataFrame,\n    ) -&gt; pd.DataFrame:\n        \"\"\"Merge max slope and mean slope results into comprehensive DataFrame.\"\"\"\n        frames = [\n            frame\n            for frame in [\n                max_slope_results,\n                mean_slope_results,\n                dormant_hf_results,\n                astm_results,\n            ]\n            if not frame.empty\n        ]\n\n        if not frames:\n            return pd.DataFrame()\n\n        combined = frames[0]\n        for frame in frames[1:]:\n            combined = pd.merge(\n                combined,\n                frame,\n                on=[\"sample\", \"sample_short\"],\n                how=\"outer\",\n                suffixes=(\"\", \"_duplicate\"),\n            )\n\n        duplicate_cols = [col for col in combined.columns if col.endswith(\"_duplicate\")]\n        combined = combined.drop(columns=duplicate_cols)\n\n        return combined\n\n    def _plot_combined_slope_analysis(\n        self,\n        results: pd.DataFrame,\n        params: ProcessingParameters,\n        target_col: str,\n        age_col: str,\n        plot_type: str,\n        regex: Optional[str],\n        plotpath: Optional[pathlib.Path],\n        ax,\n        show_plot: bool = True,\n        monochrome: bool = False,\n        marker_size: float = 1.0,\n    ):\n        \"\"\"\n        Plot combined slope analysis results based on plot_type parameter.\n\n        Parameters\n        ----------\n        results : pd.DataFrame\n            Combined results containing both max and mean slope data\n        target_col : str\n            Column name for heat flow data\n        age_col : str\n            Column name for time data\n        plot_type : str\n            Type of plot to show: 'max', 'mean', or 'both'\n            - 'max': Shows only maximum slope analysis plot\n            - 'mean': Shows only mean slope (flank tangent) analysis plot\n            - 'both': Shows both analysis types (separate plots for each)\n        regex : str, optional\n            Regex to filter samples\n        plotpath : pathlib.Path, optional\n            Path to save plots\n        cutoff_min : float, optional\n            Cutoff time in minutes\n        ax : matplotlib.axes.Axes, optional\n            Matplotlib axes to plot on\n        show_plot : bool, optional\n            Whether to show the plot, by default True\n        \"\"\"\n        # Validate plot_type parameter\n        valid_plot_types = [\"max\", \"mean\", \"both\"]\n        cutoff_min = params.cutoff.cutoff_min\n\n        if plot_type not in valid_plot_types:\n            raise ValueError(\n                f\"plot_type must be one of {valid_plot_types}, got '{plot_type}'\"\n            )\n\n        # For now, plot using the existing unified plotting approach\n        # This could be enhanced to show both slope methods simultaneously\n        for _, result_row in results.iterrows():\n            sample = result_row[\"sample\"]\n            sample_short = result_row[\"sample_short\"]\n\n            # Get sample data\n            sample_data = self._get_filtered_sample_data(\n                sample, age_col, cutoff_time_min=cutoff_min\n            )\n            if sample_data.empty:\n                continue\n\n            if not pd.isna(\n                result_row.onset_time_s_from_mean_slope or result_row.onset_time_s_from_max_slope\n            ):\n                self._plotter.plot_tangent_analysis(\n                    sample_data,\n                    sample_short,\n                    params,\n                    ax=ax,\n                    age_col=age_col,\n                    target_col=target_col,\n                    cutoff_time_min=cutoff_min,\n                    analysis_type=plot_type,  # Use correct analysis type\n                    results=result_row.to_frame().T,\n                    figsize=(7, 5),\n                    metadata=self._metadata,\n                    metadata_id=self._metadata_id,\n                    monochrome=monochrome,\n                    marker_size=marker_size,\n                )\n            self._save_and_show_plot(\n                plotpath, f\"{plot_type}_slope_{sample_short}.png\", ax, show_plot=show_plot\n            )\n\n    def _plot_ascending_slope_analysis(\n        self,\n        results: pd.DataFrame,\n        params: ProcessingParameters,\n        target_col: str,\n        age_col: str,\n        regex: Optional[str],\n        plotpath: Optional[pathlib.Path],\n        ax,\n        show_plot: bool = True,\n        monochrome: bool = False,\n        marker_size: float = 1.0,\n    ):\n        \"\"\"Plot first ascending slope analysis results.\"\"\"\n        cutoff_min = params.cutoff.cutoff_min\n\n        for _, result_row in results.iterrows():\n            sample = result_row[\"sample\"]\n            sample_short = result_row[\"sample_short\"]\n\n            sample_data = self._get_filtered_sample_data(\n                sample, age_col, cutoff_time_min=cutoff_min\n            )\n            if sample_data.empty:\n                continue\n\n            self._plotter.plot_tangent_analysis(\n                sample_data,\n                sample_short,\n                params,\n                ax=ax,\n                age_col=age_col,\n                target_col=target_col,\n                cutoff_time_min=cutoff_min,\n                analysis_type=\"ascending\",\n                results=result_row.to_frame().T,\n                figsize=(7, 5),\n                metadata=self._metadata,\n                metadata_id=self._metadata_id,\n                monochrome=monochrome,\n                marker_size=marker_size,\n            )\n\n            self._save_and_show_plot(\n                plotpath,\n                f\"ascending_slope_{sample_short}.png\",\n                ax,\n                show_plot=show_plot,\n            )\n\n\n    # Backward compatibility methods\n    def get_peak_onset_via_max_slope(\n        self,\n        processparams: Optional[ProcessingParameters] = None,\n        show_plot: bool = False,\n        ax=None,\n        regex: Optional[str] = None,\n        age_col: str = \"time_s\",\n        target_col: str = \"normalized_heat_flow_w_g\",\n        time_discarded_s: float = 900,\n        save_path: Optional[pathlib.Path] = None,\n        xscale: str = \"linear\",\n        xunit: str = \"s\",\n        intersection: str = \"dormant_hf\",\n    ):\n        \"\"\"\n        Get reaction onset via maximum slope intersection method.\n\n        This is a wrapper around get_peak_onset_via_slope for backward compatibility.\n        Returns only the max slope related columns for compatibility.\n        \"\"\"\n        full_results = self.get_mainpeak_params(\n            processparams=processparams,\n            target_col=target_col,\n            age_col=age_col,\n            show_plot=show_plot,\n            regex=regex,\n            ax=ax,\n            plot_type=\"max\",\n\n            #time_discarded_s=time_discarded_s,\n            #intersection=intersection,\n            #xunit=xunit,\n        )\n\n        if full_results.empty:\n            return full_results\n\n        # Extract only max slope related columns for backward compatibility\n        # max_slope_cols = [\n        #     col\n        #     for col in full_results.columns\n        #     if col.startswith(\"max_slope_\") or col in [\"sample\", \"sample_short\"]\n        # ]\n\n        # result = full_results[max_slope_cols].copy()\n\n        # Rename columns to match old API\n        # column_mapping = {\n        #     \"onset_time_s_from_max_slope\": \"onset_time_s\",\n        #     \"max_slope_onset_time_min\": \"onset_time_min\",\n        #     \"max_slope_value\": \"maximum_slope\",\n        #     \"max_slope_time_s\": \"maximum_slope_time_s\",\n        # }\n\n        # for old_name, new_name in column_mapping.items():\n        #     if old_name in result.columns:\n        #         result = result.rename(columns={old_name: new_name})\n\n        return full_results\n\n\n    def get_ascending_flank_tangent(\n        self,\n        processparams: Optional[ProcessingParameters] = None,\n        target_col: str = \"normalized_heat_flow_w_g\",\n        age_col: str = \"time_s\",\n        flank_fraction_start: float = 0.2,\n        flank_fraction_end: float = 0.8,\n        window_size: float = 0.1,\n        cutoff_min: Optional[float] = None,\n        show_plot: bool = False,\n        regex: Optional[str] = None,\n        plotpath: Optional[pathlib.Path] = None,\n        ax=None,\n    ) -&gt; pd.DataFrame:\n        \"\"\"\n        Determine tangent to ascending flank of peak by averaging over sections.\n\n        This is a wrapper around get_peak_onset_via_slope for backward compatibility.\n        Returns only the mean slope related columns for compatibility.\n        \"\"\"\n        full_results = self.get_peak_onset_via_slope(\n            processparams=processparams,\n            target_col=target_col,\n            age_col=age_col,\n            cutoff_min=cutoff_min,\n            show_plot=show_plot,\n            regex=regex,\n            plotpath=plotpath,\n            ax=ax,\n            flank_fraction_start=flank_fraction_start,\n            flank_fraction_end=flank_fraction_end,\n            window_size=window_size,\n        )\n\n        if full_results.empty:\n            return full_results\n\n        # Extract only mean slope related columns for backward compatibility\n        mean_slope_cols = [\n            col\n            for col in full_results.columns\n            if col.startswith(\"mean_slope_\")\n            or col in [\"sample\", \"sample_short\", \"peak_time_s\", \"peak_value\"]\n        ]\n\n        result = full_results[mean_slope_cols].copy()\n\n        # Rename columns to match old API\n        column_mapping = {\n            \"mean_slope_onset_time_s\": \"x_intersection\",\n            \"mean_slope_value\": \"tangent_slope\",\n            \"mean_slope_time_s\": \"tangent_time_s\",\n        }\n\n        for old_name, new_name in column_mapping.items():\n            if old_name in result.columns:\n                result = result.rename(columns={old_name: new_name})\n\n        return result\n\n    def get_dormant_period_heatflow(\n        self,\n        processparams: Optional[ProcessingParameters] = None,\n        regex: Optional[str] = None,\n        cutoff_min: int = 5,\n        upper_dormant_thresh_w_g: float = 0.002,\n        plot_right_boundary: float = 2e5,\n        prominence: float = 1e-3,\n        show_plot: bool = False,\n    ) -&gt; pd.DataFrame:\n        \"\"\"Get dormant period heat flow characteristics.\"\"\"\n        params = processparams or self.processparams\n\n        # Get peaks first\n        peaks = self.get_peaks(params, regex=regex, show_plot=False)\n\n        # Analyze dormant period\n        analyzer = DormantPeriodAnalyzer(params)\n        dorm_hf = analyzer.get_dormant_period_heatflow(\n            self._data, peaks, regex, upper_dormant_thresh_w_g\n        )\n\n        if not dorm_hf.empty:\n            return dorm_hf\n        else:\n            return pd.DataFrame()\n\n    def get_astm_c1679_characteristics(\n        self,\n        processparams: Optional[ProcessingParameters] = None,\n        individual: bool = True,\n        show_plot: bool = False,\n        ax=None,\n        regex: Optional[str] = None,\n        xscale: str = \"log\",\n        xunit: str = \"s\",\n    ) -&gt; pd.DataFrame:\n        \"\"\"Get characteristics according to ASTM C1679.\"\"\"\n        params = processparams or self.processparams\n\n        peaks = self.get_peaks(params, regex=regex, show_plot=False)\n\n        analyzer = ASTMC1679Analyzer(params)\n        df = analyzer.get_astm_c1679_characteristics(\n            self._data, peaks, individual, regex\n        )\n\n        if show_plot and not df.empty:\n            self._plotter.plot_astm_c1679(self._data, df, ax, xunit)\n\n        return df\n\n    def get_cumulated_heat_at_hours(\n        self,\n        processparams: Optional[ProcessingParameters] = None,\n        target_h: float = 4,\n        **kwargs,\n    ) -&gt; pd.DataFrame:\n        \"\"\"Get cumulated heat flow at specific age.\"\"\"\n        if \"cutoff_min\" in kwargs:\n            cutoff_min = kwargs[\"cutoff_min\"]\n            warnings.warn(\n                \"The cutoff_min parameter is deprecated. Use ProcessingParameters instead.\",\n                DeprecationWarning,\n                stacklevel=2,\n            )\n        else:\n            params = processparams or self.processparams\n            cutoff_min = params.cutoff.cutoff_min\n\n        return HeatCalculator.get_cumulated_heat_at_hours(\n            self._data, target_h, cutoff_min\n        )\n\n    def get_average_slope(\n        self,\n        processparams: Optional[ProcessingParameters] = None,\n        target_col: str = \"normalized_heat_flow_w_g\",\n        age_col: str = \"time_s\",\n        regex: Optional[str] = None,\n        show_plot: bool = False,\n        ax=None,\n        save_path: Optional[pathlib.Path] = None,\n        xunit: str = \"s\",\n    ) -&gt; pd.DataFrame:\n        \"\"\"Calculate the mean (flank tangent) slope of the main hydration peak.\n\n        Wrapper around :meth:`get_mainpeak_params` returning only the\n        ``mean_slope`` columns together with sample identifiers.\n        \"\"\"\n        result = self.get_mainpeak_params(\n            processparams=processparams,\n            target_col=target_col,\n            age_col=age_col,\n            show_plot=show_plot,\n            plot_type=\"mean\",\n            regex=regex,\n            ax=ax,\n        )\n        if result.empty:\n            return result\n        id_cols = [\"sample\", \"sample_short\"]\n        mean_slope_cols = [c for c in result.columns if \"mean_slope\" in c]\n        return result[id_cols + mean_slope_cols].reset_index(drop=True)\n\n    def _plot_tangent_analysis_unified(\n        self,\n        results: pd.DataFrame,\n        analysis_type: str,\n        target_col: str,\n        age_col: str,\n        regex: Optional[str] = None,\n        plotpath: Optional[pathlib.Path] = None,\n        cutoff_time_min: Optional[float] = None,\n        intersection: str = \"dormant_hf\",\n        xunit: str = \"s\",\n        time_discarded_s: float = 900,\n        ax=None,\n        # Additional data for onset intersection analysis\n        max_slopes: Optional[pd.DataFrame] = None,\n        dormant_hfs: Optional[pd.DataFrame] = None,\n        onsets: Optional[pd.DataFrame] = None,\n    ):\n        \"\"\"\n        Unified plotting method for tangent-based analysis results.\n\n        This method handles both flank tangent and onset intersection analysis,\n        with the main difference being how the slope is determined:\n        - Flank tangent: Uses averaged slope over a window\n        - Max slope: Uses single point with maximum gradient\n\n        Parameters\n        ----------\n        results : pd.DataFrame\n            Results from the analysis (tangent results for flank, onsets for max slope)\n        analysis_type : str\n            Either 'flank_tangent' or 'max_slope_onset'\n        target_col : str\n            Column name for heat flow data\n        age_col : str\n            Column name for time data\n        regex : str, optional\n            Regex to filter samples\n        plotpath : pathlib.Path, optional\n            Path to save plots\n        cutoff_time_min : float, optional\n            Cutoff time in minutes\n        intersection : str\n            Type of intersection for onset analysis ('dormant_hf' or 'abscissa')\n        xunit : str\n            Time unit for plotting\n        time_discarded_s : float\n            Time to discard for onset analysis\n        ax : matplotlib.axes.Axes, optional\n            Matplotlib axes to plot on\n        max_slopes : pd.DataFrame, optional\n            Required for onset intersection analysis\n        dormant_hfs : pd.DataFrame, optional\n            Required for onset intersection analysis with dormant_hf\n        onsets : pd.DataFrame, optional\n            Required for onset intersection analysis\n        \"\"\"\n        try:\n            if analysis_type == \"flank_tangent\":\n                self._plot_flank_tangent_unified(\n                    results, target_col, age_col, regex, plotpath, cutoff_time_min, ax\n                )\n            elif analysis_type == \"max_slope_onset\":\n                self._plot_onset_intersection_unified(\n                    results,\n                    max_slopes,\n                    dormant_hfs,\n                    target_col,\n                    age_col,\n                    regex,\n                    intersection,\n                    xunit,\n                    time_discarded_s,\n                    ax,\n                )\n            else:\n                raise ValueError(f\"Unknown analysis_type: {analysis_type}\")\n\n        except Exception as e:\n            logger.error(f\"Error plotting tangent analysis results: {e}\")\n            print(f\"Plotting failed: {e}\")\n\n    def _plot_flank_tangent_unified(\n        self,\n        results: pd.DataFrame,\n        target_col: str,\n        age_col: str,\n        regex: Optional[str] = None,\n        plotpath: Optional[pathlib.Path] = None,\n        cutoff_time_min: Optional[float] = None,\n        ax=None,\n    ):\n        \"\"\"Plot flank tangent analysis results using unified SimplePlotter.\"\"\"\n        for _, result_row in results.iterrows():\n            sample = result_row[\"sample\"]\n            sample_short = result_row[\"sample_short\"]\n\n            # Get sample data\n            sample_data = self._get_filtered_sample_data(\n                sample, age_col, cutoff_time_min=cutoff_time_min\n            )\n            if sample_data.empty:\n                continue\n\n            # Create a DataFrame with just this result for plotting\n            single_result = pd.DataFrame([result_row])\n\n            # Use unified plotting method\n            self._plotter.plot_tangent_analysis(\n                sample_data,\n                sample_short,\n                ax=ax,\n                age_col=age_col,\n                target_col=target_col,\n                cutoff_time_min=cutoff_time_min,\n                analysis_type=\"flank_tangent\",\n                tangent_results=single_result,\n                figsize=(7, 5),\n            )\n\n            self._save_and_show_plot(plotpath, f\"flank_tangent_{sample_short}.png\", ax)\n\n    def _plot_onset_intersection_unified(\n        self,\n        onsets: pd.DataFrame,\n        max_slopes: Optional[pd.DataFrame],\n        dormant_hfs: Optional[pd.DataFrame],\n        target_col: str,\n        age_col: str,\n        regex: Optional[str] = None,\n        intersection: str = \"dormant_hf\",\n        xunit: str = \"s\",\n        time_discarded_s: float = 900,\n        ax=None,\n    ):\n        \"\"\"Plot onset intersection analysis results using unified SimplePlotter.\"\"\"\n        if max_slopes is None:\n            raise ValueError(\"max_slopes required for onset intersection analysis\")\n\n        for _, onset_row in onsets.iterrows():\n            sample = onset_row[\"sample\"]\n\n            # Get sample data\n            sample_data = self._get_filtered_sample_data(\n                sample, age_col, time_discarded_s=time_discarded_s\n            )\n            if sample_data.empty:\n                continue\n\n            # Use unified plotting method\n            self._plotter.plot_tangent_analysis(\n                sample_data,\n                sample,\n                ax=ax,\n                age_col=age_col,\n                target_col=target_col,\n                analysis_type=\"onset_intersection\",\n                max_slopes=max_slopes,\n                dormant_hfs=dormant_hfs,\n                onsets=onsets,\n                intersection=intersection,\n                xunit=xunit,\n                figsize=(12, 8),\n            )\n\n            # Note: plotpath not available in this context, only show plot\n            self._save_and_show_plot(None, f\"onset_intersection_{sample}.png\", ax)\n\n    def _get_filtered_sample_data(\n        self,\n        sample: str,\n        age_col: str,\n        cutoff_time_min: Optional[float] = None,\n        time_discarded_s: Optional[float] = None,\n    ) -&gt; pd.DataFrame:\n        \"\"\"\n        Get sample data with appropriate filtering applied.\n\n        This consolidates the common data filtering logic used in both analysis types.\n        \"\"\"\n        # Get sample data - handle both 'sample' and 'sample_short' columns\n        sample_data = self._data[\n            (self._data[\"sample\"] == sample)\n            | (self._data.get(\"sample_short\", \"\") == sample)\n        ]\n\n        if sample_data.empty:\n            return sample_data\n\n        # Apply cutoff time filtering\n        if cutoff_time_min is not None:\n            cutoff_seconds = cutoff_time_min * 60\n            sample_data = sample_data[sample_data[age_col] &gt;= cutoff_seconds]\n\n        # Apply time discarded filtering (for onset analysis)\n        if time_discarded_s is not None and time_discarded_s &gt; 0:\n            sample_data = sample_data[sample_data[age_col] &gt;= time_discarded_s]\n\n        return sample_data\n\n    def _save_and_show_plot(self, plotpath: Optional[pathlib.Path], filename: str, ax, show_plot: bool = True):\n        \"\"\"Handle plot saving and showing - common logic for both analysis types.\"\"\"\n        import matplotlib.pyplot as plt\n\n        if plotpath:\n            plot_file = plotpath / filename\n            plt.savefig(plot_file, dpi=300, bbox_inches=\"tight\")\n\n        if not ax:\n            if show_plot:\n                plt.show()\n            else:\n                plt.close()\n\n    def _plot_flank_tangent_results(\n        self,\n        results: pd.DataFrame,\n        target_col: str,\n        age_col: str,\n        regex: Optional[str] = None,\n        plotpath: Optional[pathlib.Path] = None,\n        cutoff_time_min: Optional[float] = None,\n        ax=None,\n    ):\n        \"\"\"\n        Plot flank tangent analysis results using SimplePlotter.\n\n        This is a wrapper around the unified plotting method for backward compatibility.\n        \"\"\"\n        return self._plot_tangent_analysis_unified(\n            results=results,\n            analysis_type=\"flank_tangent\",\n            target_col=target_col,\n            age_col=age_col,\n            regex=regex,\n            plotpath=plotpath,\n            cutoff_time_min=cutoff_time_min,\n            ax=ax,\n        )\n\n    def _plot_onset_intersections(\n        self,\n        onsets: pd.DataFrame,\n        max_slopes: pd.DataFrame,\n        dormant_hfs: pd.DataFrame,\n        target_col: str,\n        age_col: str,\n        regex: Optional[str] = None,\n        intersection: str = \"dormant_hf\",\n        xunit: str = \"s\",\n        time_discarded_s: float = 900,\n        ax=None,\n    ):\n        \"\"\"\n        Plot onset intersection analysis results using SimplePlotter.\n\n        This is a wrapper around the unified plotting method for backward compatibility.\n        \"\"\"\n        return self._plot_tangent_analysis_unified(\n            results=onsets,\n            analysis_type=\"max_slope_onset\",\n            target_col=target_col,\n            age_col=age_col,\n            regex=regex,\n            intersection=intersection,\n            xunit=xunit,\n            time_discarded_s=time_discarded_s,\n            ax=ax,\n            max_slopes=max_slopes,\n            dormant_hfs=dormant_hfs,\n            onsets=onsets,\n        )\n\n    # Data manipulation methods\n    def normalize_sample_to_mass(\n        self, sample_short: str, mass_g: float, show_info: bool = True\n    ):\n        \"\"\"Normalize heat flow values to a specific mass.\"\"\"\n        self._data = DataNormalizer.normalize_sample_to_mass(\n            self._data, sample_short, mass_g, show_info\n        )\n\n    def apply_tian_correction(\n        self, processparams: Optional[ProcessingParameters] = None\n    ) -&gt; None:\n        \"\"\"\n        Apply Tian correction to the heat flow data.\n\n        Corrects the measured heat flow for the thermal inertia of the calorimeter\n        using one or two time constants (tau1, tau2) from processparams.\n\n        Single time constant (tau2 = None):\n            hf_corrected = dHF/dt * tau1 + HF\n\n        Dual time constants:\n            hf_corrected = dHF/dt * (tau1 + tau2) + d\u00b2HF/dt\u00b2 * tau1*tau2 + HF\n\n        Results are written to three new columns:\n        - ``normalized_heat_flow_w_g_tian``\n        - ``gradient_normalized_heat_flow_w_g``\n        - ``normalized_heat_j_g_tian``\n\n        Parameters\n        ----------\n        processparams : ProcessingParameters, optional\n            Processing parameters containing time_constants.tau1 and\n            time_constants.tau2. Uses instance processparams if not provided.\n        \"\"\"\n        from scipy import integrate\n\n        if processparams is None:\n            processparams = self._processparams\n\n        for s, sample_data in SampleIterator.iter_samples(self._data):\n            processor = HeatFlowProcessor(processparams)\n            gradient, curvature = processor.calculate_heatflow_derivatives(sample_data)\n\n            hf = sample_data[\"normalized_heat_flow_w_g\"].to_numpy()\n            x = sample_data[\"time_s\"].to_numpy()\n            tau1 = processparams.time_constants.tau1\n\n            if processparams.time_constants.tau2 is None:\n                norm_hf = gradient * tau1 + hf\n            else:\n                tau2 = processparams.time_constants.tau2\n                norm_hf = gradient * (tau1 + tau2) + curvature * tau1 * tau2 + hf\n\n            mask = self._data[\"sample\"] == s\n            self._data.loc[mask, \"normalized_heat_flow_w_g_tian\"] = norm_hf\n            self._data.loc[mask, \"gradient_normalized_heat_flow_w_g\"] = gradient\n            self._data.loc[mask, \"normalized_heat_j_g_tian\"] = (\n                integrate.cumulative_trapezoid(\n                    np.nan_to_num(norm_hf), x=x, initial=0\n                )\n            )\n\n    def add_metadata_source(\n        self,\n        file: Union[str, pathlib.Path],\n        sample_id_column: str,\n        show_info: bool = True,\n    ):\n        \"\"\"\n        Add metadata from external source (CSV or Excel file).\n\n        Parameters\n        ----------\n        file : str or pathlib.Path\n            Path to metadata file (CSV, Excel, etc.)\n        sample_id_column : str\n            Column name in metadata file that matches sample names\n        show_info : bool, optional\n            Whether to print informative messages, by default True\n\n        Raises\n        ------\n        FileNotFoundError\n            If the metadata file does not exist\n        ValueError\n            If the sample_id_column is not found in the metadata file\n        \"\"\"\n        file_path = pathlib.Path(file)\n\n        if not file_path.exists():\n            raise FileNotFoundError(f\"Metadata file not found: {file_path}\")\n\n        # Load metadata based on file extension\n        try:\n            if file_path.suffix.lower() in [\".xlsx\", \".xls\"]:\n                self._metadata = pd.read_excel(file_path)\n            elif file_path.suffix.lower() == \".csv\":\n                self._metadata = pd.read_csv(file_path)\n            else:\n                raise ValueError(\n                    f\"Unsupported file format: {file_path.suffix}. Use CSV or Excel files.\"\n                )\n\n            if show_info:\n                print(f\"Loaded metadata from: {file_path}\")\n                print(f\"Metadata shape: {self._metadata.shape}\")\n\n        except Exception as e:\n            logger.error(f\"Error loading metadata file: {e}\")\n            raise ValueError(f\"Failed to load metadata from {file_path}: {e}\")\n\n        # Validate sample_id_column\n        if sample_id_column not in self._metadata.columns:\n            raise ValueError(\n                f\"Column '{sample_id_column}' not found in metadata. \"\n                f\"Available columns: {list(self._metadata.columns)}\"\n            )\n\n        self._metadata_id = sample_id_column\n\n        # Try to match metadata with existing samples\n        if not self._data.empty and \"sample_short\" in self._data.columns:\n            sample_names = self.get_sample_names()\n            metadata_ids = self._metadata[sample_id_column].unique()\n\n            matched = set(sample_names) &amp; set(metadata_ids)\n            unmatched_samples = set(sample_names) - matched\n            unmatched_metadata = set(metadata_ids) - matched\n\n            if show_info:\n                print(\"\\nMetadata matching results:\")\n                print(f\"  Matched samples: {len(matched)}\")\n                if unmatched_samples:\n                    print(f\"  Unmatched samples: {len(unmatched_samples)}\")\n                    print(f\"    {list(unmatched_samples)[:5]}{'...' if len(unmatched_samples) &gt; 5 else ''}\")\n                if unmatched_metadata:\n                    print(f\"  Unmatched metadata entries: {len(unmatched_metadata)}\")\n\n        if show_info:\n            print(f\"Metadata successfully added with ID column: '{sample_id_column}'\")\n\n    def average_by_metadata(\n        self,\n        groupby: str | list[str],\n        bin_width_s: int = 60,\n    ) -&gt; None:\n        \"\"\"Replace individual samples with group averages defined by metadata.\n\n        Requires :meth:`add_metadata_source` to have been called first.\n        The averaged data replaces ``self._data`` in-place so that all\n        downstream methods (``plot``, ``get_cumulated_heat_at_hours``, \u2026)\n        operate on the grouped curves.  Call :meth:`undo_average_by_metadata`\n        to restore the original data.\n\n        Parameters\n        ----------\n        groupby : str or list of str\n            Metadata column(s) to group by, e.g. ``\"cement_name\"`` or\n            ``[\"cement_name\", \"cement_amount_g\"]``.\n        bin_width_s : int\n            Width of each time bin in seconds. Default is 60 s.\n        \"\"\"\n        if self._metadata.empty:\n            raise ValueError(\n                \"No metadata loaded. Call add_metadata_source() first.\"\n            )\n        self._data_before_average = self._data.copy()\n        self._data = MetadataAggregator.average_by_metadata(\n            self._data,\n            self._metadata,\n            self._metadata_id,\n            groupby,\n            bin_width_s,\n        )\n\n    def undo_average_by_metadata(self) -&gt; None:\n        \"\"\"Restore the original per-sample data after average_by_metadata.\"\"\"\n        if not hasattr(self, \"_data_before_average\"):\n            raise ValueError(\n                \"No averaged data to undo. Call average_by_metadata() first.\"\n            )\n        self._data = self._data_before_average\n        del self._data_before_average\n\n    def remove_pickle_files(self):\n        \"\"\"Remove pickle cache files.\"\"\"\n        self._data_persistence.remove_pickle_files()\n\n    # Private utility methods\n    def _iter_samples(self, regex: Optional[str] = None):\n        \"\"\"Iterate over samples - compatibility method.\"\"\"\n        return SampleIterator.iter_samples(self._data, regex)\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.__init__","title":"<code>__init__(folder=None, show_info=True, regex=None, auto_clean=False, cold_start=True, processparams=None, new_code=False, processed=False, metadata_path=None, metadata_id_column=None, save_cache=False)</code>","text":"<p>Initialize measurements from folder or existing data.</p> <p>Parameters:</p> Name Type Description Default <code>folder</code> <code>str or Path</code> <p>Path to folder containing experimental files</p> <code>None</code> <code>show_info</code> <code>bool</code> <p>Whether to print informative messages, by default True</p> <code>True</code> <code>regex</code> <code>str</code> <p>Regex pattern to filter files, by default None</p> <code>None</code> <code>auto_clean</code> <code>bool</code> <p>Whether to clean data automatically, by default False</p> <code>False</code> <code>cold_start</code> <code>bool</code> <p>Whether to read from files or use cached data, by default True</p> <code>True</code> <code>processparams</code> <code>ProcessingParameters</code> <p>Processing parameters, by default None. If None, the default parameters will be used</p> <code>None</code> <code>new_code</code> <code>bool</code> <p>Flag for new code features, by default False</p> <code>False</code> <code>processed</code> <code>bool</code> <p>Whether data is already processed, i.e., if a .csv file is used which was processed  by Calocem. By default False</p> <code>False</code> <code>metadata_path</code> <code>str or Path</code> <p>Path to metadata file (CSV, Excel, etc.), by default None</p> <code>None</code> <code>metadata_id_column</code> <code>str</code> <p>Column name in metadata file that matches sample names, by default None</p> <code>None</code> <code>save_cache</code> <code>bool</code> <p>Whether to write <code>_data.pickle</code> and <code>_info.pickle</code> cache files when loading from a folder, by default False. When True, subsequent runs can be sped up with <code>cold_start=False</code> to read from the cache instead of re-parsing the folder. When False (default), no pickle files are created.</p> <code>False</code> Source code in <code>calocem/measurement.py</code> <pre><code>def __init__(\n    self,\n    folder: Optional[Union[str, pathlib.Path]] = None,\n    show_info: bool = True,\n    regex: Optional[str] = None,\n    auto_clean: bool = False,\n    cold_start: bool = True,\n    processparams: Optional[ProcessingParameters] = None,\n    new_code: bool = False,\n    processed: bool = False,\n    metadata_path: Optional[Union[str, pathlib.Path]] = None,\n    metadata_id_column: Optional[str] = None,\n    save_cache: bool = False,\n):\n    \"\"\"\n    Initialize measurements from folder or existing data.\n\n    Parameters\n    ----------\n    folder : str or pathlib.Path, optional\n        Path to folder containing experimental files\n    show_info : bool, optional\n        Whether to print informative messages, by default True\n    regex : str, optional\n        Regex pattern to filter files, by default None\n    auto_clean : bool, optional\n        Whether to clean data automatically, by default False\n    cold_start : bool, optional\n        Whether to read from files or use cached data, by default True\n    processparams : ProcessingParameters, optional\n        Processing parameters, by default None. If None, the default parameters will be used\n    new_code : bool, optional\n        Flag for new code features, by default False\n    processed : bool, optional\n        Whether data is already processed, i.e., if a .csv file is used which was processed  by Calocem. By default False\n    metadata_path : str or pathlib.Path, optional\n        Path to metadata file (CSV, Excel, etc.), by default None\n    metadata_id_column : str, optional\n        Column name in metadata file that matches sample names, by default None\n    save_cache : bool, optional\n        Whether to write `_data.pickle` and `_info.pickle` cache files when loading\n        from a folder, by default False. When True, subsequent runs can be sped up\n        with ``cold_start=False`` to read from the cache instead of re-parsing the\n        folder. When False (default), no pickle files are created.\n    \"\"\"\n    # Initialize attributes\n    self._data = pd.DataFrame()\n    self._info = pd.DataFrame()\n    self._data_unprocessed = pd.DataFrame()\n    self._metadata = pd.DataFrame()\n    self._metadata_id = \"\"\n\n    # Store configuration\n    self._new_code = new_code\n    self._processed = processed\n    self._save_cache = save_cache\n\n    # Setup processing parameters\n    if not isinstance(processparams, ProcessingParameters):\n        self.processparams = ProcessingParameters()\n    else:\n        self.processparams = processparams\n\n    # Initialize components\n    self._folder_loader = FolderDataLoader(processed=processed)\n    self._data_persistence = DataPersistence()\n    self._data_cleaner = DataCleaner()\n    self._plotter = SimplePlotter()\n\n    # Load data if folder provided\n    if folder:\n        try:\n            if cold_start:\n                self._load_from_folder(folder, regex, show_info)\n            else:\n                self._load_from_cache()\n\n            if auto_clean:\n                self._auto_clean_data()\n\n        except Exception as e:\n            if show_info:\n                print(f\"Error during initialization: {e}\")\n            if auto_clean:\n                raise AutoCleanException()\n            if not cold_start:\n                raise ColdStartException()\n            raise\n\n    # Load metadata if provided\n    if metadata_path and metadata_id_column:\n        self.add_metadata_source(metadata_path, metadata_id_column, show_info)\n\n    # Apply downsampling if requested\n    if self.processparams.downsample.apply:\n        self._apply_adaptive_downsampling()\n\n    # Information message\n    if show_info:\n        print(\"================\")\n        print(\n            \"Are you missing some samples? Try rerunning with auto_clean=True and cold_start=True.\"\n        )\n        print(\"================\")\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.add_metadata_source","title":"<code>add_metadata_source(file, sample_id_column, show_info=True)</code>","text":"<p>Add metadata from external source (CSV or Excel file).</p> <p>Parameters:</p> Name Type Description Default <code>file</code> <code>str or Path</code> <p>Path to metadata file (CSV, Excel, etc.)</p> required <code>sample_id_column</code> <code>str</code> <p>Column name in metadata file that matches sample names</p> required <code>show_info</code> <code>bool</code> <p>Whether to print informative messages, by default True</p> <code>True</code> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If the metadata file does not exist</p> <code>ValueError</code> <p>If the sample_id_column is not found in the metadata file</p> Source code in <code>calocem/measurement.py</code> <pre><code>def add_metadata_source(\n    self,\n    file: Union[str, pathlib.Path],\n    sample_id_column: str,\n    show_info: bool = True,\n):\n    \"\"\"\n    Add metadata from external source (CSV or Excel file).\n\n    Parameters\n    ----------\n    file : str or pathlib.Path\n        Path to metadata file (CSV, Excel, etc.)\n    sample_id_column : str\n        Column name in metadata file that matches sample names\n    show_info : bool, optional\n        Whether to print informative messages, by default True\n\n    Raises\n    ------\n    FileNotFoundError\n        If the metadata file does not exist\n    ValueError\n        If the sample_id_column is not found in the metadata file\n    \"\"\"\n    file_path = pathlib.Path(file)\n\n    if not file_path.exists():\n        raise FileNotFoundError(f\"Metadata file not found: {file_path}\")\n\n    # Load metadata based on file extension\n    try:\n        if file_path.suffix.lower() in [\".xlsx\", \".xls\"]:\n            self._metadata = pd.read_excel(file_path)\n        elif file_path.suffix.lower() == \".csv\":\n            self._metadata = pd.read_csv(file_path)\n        else:\n            raise ValueError(\n                f\"Unsupported file format: {file_path.suffix}. Use CSV or Excel files.\"\n            )\n\n        if show_info:\n            print(f\"Loaded metadata from: {file_path}\")\n            print(f\"Metadata shape: {self._metadata.shape}\")\n\n    except Exception as e:\n        logger.error(f\"Error loading metadata file: {e}\")\n        raise ValueError(f\"Failed to load metadata from {file_path}: {e}\")\n\n    # Validate sample_id_column\n    if sample_id_column not in self._metadata.columns:\n        raise ValueError(\n            f\"Column '{sample_id_column}' not found in metadata. \"\n            f\"Available columns: {list(self._metadata.columns)}\"\n        )\n\n    self._metadata_id = sample_id_column\n\n    # Try to match metadata with existing samples\n    if not self._data.empty and \"sample_short\" in self._data.columns:\n        sample_names = self.get_sample_names()\n        metadata_ids = self._metadata[sample_id_column].unique()\n\n        matched = set(sample_names) &amp; set(metadata_ids)\n        unmatched_samples = set(sample_names) - matched\n        unmatched_metadata = set(metadata_ids) - matched\n\n        if show_info:\n            print(\"\\nMetadata matching results:\")\n            print(f\"  Matched samples: {len(matched)}\")\n            if unmatched_samples:\n                print(f\"  Unmatched samples: {len(unmatched_samples)}\")\n                print(f\"    {list(unmatched_samples)[:5]}{'...' if len(unmatched_samples) &gt; 5 else ''}\")\n            if unmatched_metadata:\n                print(f\"  Unmatched metadata entries: {len(unmatched_metadata)}\")\n\n    if show_info:\n        print(f\"Metadata successfully added with ID column: '{sample_id_column}'\")\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.apply_tian_correction","title":"<code>apply_tian_correction(processparams=None)</code>","text":"<p>Apply Tian correction to the heat flow data.</p> <p>Corrects the measured heat flow for the thermal inertia of the calorimeter using one or two time constants (tau1, tau2) from processparams.</p> <p>Single time constant (tau2 = None):     hf_corrected = dHF/dt * tau1 + HF</p> <p>Dual time constants:     hf_corrected = dHF/dt * (tau1 + tau2) + d\u00b2HF/dt\u00b2 * tau1*tau2 + HF</p> <p>Results are written to three new columns: - <code>normalized_heat_flow_w_g_tian</code> - <code>gradient_normalized_heat_flow_w_g</code> - <code>normalized_heat_j_g_tian</code></p> <p>Parameters:</p> Name Type Description Default <code>processparams</code> <code>ProcessingParameters</code> <p>Processing parameters containing time_constants.tau1 and time_constants.tau2. Uses instance processparams if not provided.</p> <code>None</code> Source code in <code>calocem/measurement.py</code> <pre><code>def apply_tian_correction(\n    self, processparams: Optional[ProcessingParameters] = None\n) -&gt; None:\n    \"\"\"\n    Apply Tian correction to the heat flow data.\n\n    Corrects the measured heat flow for the thermal inertia of the calorimeter\n    using one or two time constants (tau1, tau2) from processparams.\n\n    Single time constant (tau2 = None):\n        hf_corrected = dHF/dt * tau1 + HF\n\n    Dual time constants:\n        hf_corrected = dHF/dt * (tau1 + tau2) + d\u00b2HF/dt\u00b2 * tau1*tau2 + HF\n\n    Results are written to three new columns:\n    - ``normalized_heat_flow_w_g_tian``\n    - ``gradient_normalized_heat_flow_w_g``\n    - ``normalized_heat_j_g_tian``\n\n    Parameters\n    ----------\n    processparams : ProcessingParameters, optional\n        Processing parameters containing time_constants.tau1 and\n        time_constants.tau2. Uses instance processparams if not provided.\n    \"\"\"\n    from scipy import integrate\n\n    if processparams is None:\n        processparams = self._processparams\n\n    for s, sample_data in SampleIterator.iter_samples(self._data):\n        processor = HeatFlowProcessor(processparams)\n        gradient, curvature = processor.calculate_heatflow_derivatives(sample_data)\n\n        hf = sample_data[\"normalized_heat_flow_w_g\"].to_numpy()\n        x = sample_data[\"time_s\"].to_numpy()\n        tau1 = processparams.time_constants.tau1\n\n        if processparams.time_constants.tau2 is None:\n            norm_hf = gradient * tau1 + hf\n        else:\n            tau2 = processparams.time_constants.tau2\n            norm_hf = gradient * (tau1 + tau2) + curvature * tau1 * tau2 + hf\n\n        mask = self._data[\"sample\"] == s\n        self._data.loc[mask, \"normalized_heat_flow_w_g_tian\"] = norm_hf\n        self._data.loc[mask, \"gradient_normalized_heat_flow_w_g\"] = gradient\n        self._data.loc[mask, \"normalized_heat_j_g_tian\"] = (\n            integrate.cumulative_trapezoid(\n                np.nan_to_num(norm_hf), x=x, initial=0\n            )\n        )\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.average_by_metadata","title":"<code>average_by_metadata(groupby, bin_width_s=60)</code>","text":"<p>Replace individual samples with group averages defined by metadata.</p> <p>Requires :meth:<code>add_metadata_source</code> to have been called first. The averaged data replaces <code>self._data</code> in-place so that all downstream methods (<code>plot</code>, <code>get_cumulated_heat_at_hours</code>, \u2026) operate on the grouped curves.  Call :meth:<code>undo_average_by_metadata</code> to restore the original data.</p> <p>Parameters:</p> Name Type Description Default <code>groupby</code> <code>str or list of str</code> <p>Metadata column(s) to group by, e.g. <code>\"cement_name\"</code> or <code>[\"cement_name\", \"cement_amount_g\"]</code>.</p> required <code>bin_width_s</code> <code>int</code> <p>Width of each time bin in seconds. Default is 60 s.</p> <code>60</code> Source code in <code>calocem/measurement.py</code> <pre><code>def average_by_metadata(\n    self,\n    groupby: str | list[str],\n    bin_width_s: int = 60,\n) -&gt; None:\n    \"\"\"Replace individual samples with group averages defined by metadata.\n\n    Requires :meth:`add_metadata_source` to have been called first.\n    The averaged data replaces ``self._data`` in-place so that all\n    downstream methods (``plot``, ``get_cumulated_heat_at_hours``, \u2026)\n    operate on the grouped curves.  Call :meth:`undo_average_by_metadata`\n    to restore the original data.\n\n    Parameters\n    ----------\n    groupby : str or list of str\n        Metadata column(s) to group by, e.g. ``\"cement_name\"`` or\n        ``[\"cement_name\", \"cement_amount_g\"]``.\n    bin_width_s : int\n        Width of each time bin in seconds. Default is 60 s.\n    \"\"\"\n    if self._metadata.empty:\n        raise ValueError(\n            \"No metadata loaded. Call add_metadata_source() first.\"\n        )\n    self._data_before_average = self._data.copy()\n    self._data = MetadataAggregator.average_by_metadata(\n        self._data,\n        self._metadata,\n        self._metadata_id,\n        groupby,\n        bin_width_s,\n    )\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.get_ascending_flank_tangent","title":"<code>get_ascending_flank_tangent(processparams=None, target_col='normalized_heat_flow_w_g', age_col='time_s', flank_fraction_start=0.2, flank_fraction_end=0.8, window_size=0.1, cutoff_min=None, show_plot=False, regex=None, plotpath=None, ax=None)</code>","text":"<p>Determine tangent to ascending flank of peak by averaging over sections.</p> <p>This is a wrapper around get_peak_onset_via_slope for backward compatibility. Returns only the mean slope related columns for compatibility.</p> Source code in <code>calocem/measurement.py</code> <pre><code>def get_ascending_flank_tangent(\n    self,\n    processparams: Optional[ProcessingParameters] = None,\n    target_col: str = \"normalized_heat_flow_w_g\",\n    age_col: str = \"time_s\",\n    flank_fraction_start: float = 0.2,\n    flank_fraction_end: float = 0.8,\n    window_size: float = 0.1,\n    cutoff_min: Optional[float] = None,\n    show_plot: bool = False,\n    regex: Optional[str] = None,\n    plotpath: Optional[pathlib.Path] = None,\n    ax=None,\n) -&gt; pd.DataFrame:\n    \"\"\"\n    Determine tangent to ascending flank of peak by averaging over sections.\n\n    This is a wrapper around get_peak_onset_via_slope for backward compatibility.\n    Returns only the mean slope related columns for compatibility.\n    \"\"\"\n    full_results = self.get_peak_onset_via_slope(\n        processparams=processparams,\n        target_col=target_col,\n        age_col=age_col,\n        cutoff_min=cutoff_min,\n        show_plot=show_plot,\n        regex=regex,\n        plotpath=plotpath,\n        ax=ax,\n        flank_fraction_start=flank_fraction_start,\n        flank_fraction_end=flank_fraction_end,\n        window_size=window_size,\n    )\n\n    if full_results.empty:\n        return full_results\n\n    # Extract only mean slope related columns for backward compatibility\n    mean_slope_cols = [\n        col\n        for col in full_results.columns\n        if col.startswith(\"mean_slope_\")\n        or col in [\"sample\", \"sample_short\", \"peak_time_s\", \"peak_value\"]\n    ]\n\n    result = full_results[mean_slope_cols].copy()\n\n    # Rename columns to match old API\n    column_mapping = {\n        \"mean_slope_onset_time_s\": \"x_intersection\",\n        \"mean_slope_value\": \"tangent_slope\",\n        \"mean_slope_time_s\": \"tangent_time_s\",\n    }\n\n    for old_name, new_name in column_mapping.items():\n        if old_name in result.columns:\n            result = result.rename(columns={old_name: new_name})\n\n    return result\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.get_astm_c1679_characteristics","title":"<code>get_astm_c1679_characteristics(processparams=None, individual=True, show_plot=False, ax=None, regex=None, xscale='log', xunit='s')</code>","text":"<p>Get characteristics according to ASTM C1679.</p> Source code in <code>calocem/measurement.py</code> <pre><code>def get_astm_c1679_characteristics(\n    self,\n    processparams: Optional[ProcessingParameters] = None,\n    individual: bool = True,\n    show_plot: bool = False,\n    ax=None,\n    regex: Optional[str] = None,\n    xscale: str = \"log\",\n    xunit: str = \"s\",\n) -&gt; pd.DataFrame:\n    \"\"\"Get characteristics according to ASTM C1679.\"\"\"\n    params = processparams or self.processparams\n\n    peaks = self.get_peaks(params, regex=regex, show_plot=False)\n\n    analyzer = ASTMC1679Analyzer(params)\n    df = analyzer.get_astm_c1679_characteristics(\n        self._data, peaks, individual, regex\n    )\n\n    if show_plot and not df.empty:\n        self._plotter.plot_astm_c1679(self._data, df, ax, xunit)\n\n    return df\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.get_average_slope","title":"<code>get_average_slope(processparams=None, target_col='normalized_heat_flow_w_g', age_col='time_s', regex=None, show_plot=False, ax=None, save_path=None, xunit='s')</code>","text":"<p>Calculate the mean (flank tangent) slope of the main hydration peak.</p> <p>Wrapper around :meth:<code>get_mainpeak_params</code> returning only the <code>mean_slope</code> columns together with sample identifiers.</p> Source code in <code>calocem/measurement.py</code> <pre><code>def get_average_slope(\n    self,\n    processparams: Optional[ProcessingParameters] = None,\n    target_col: str = \"normalized_heat_flow_w_g\",\n    age_col: str = \"time_s\",\n    regex: Optional[str] = None,\n    show_plot: bool = False,\n    ax=None,\n    save_path: Optional[pathlib.Path] = None,\n    xunit: str = \"s\",\n) -&gt; pd.DataFrame:\n    \"\"\"Calculate the mean (flank tangent) slope of the main hydration peak.\n\n    Wrapper around :meth:`get_mainpeak_params` returning only the\n    ``mean_slope`` columns together with sample identifiers.\n    \"\"\"\n    result = self.get_mainpeak_params(\n        processparams=processparams,\n        target_col=target_col,\n        age_col=age_col,\n        show_plot=show_plot,\n        plot_type=\"mean\",\n        regex=regex,\n        ax=ax,\n    )\n    if result.empty:\n        return result\n    id_cols = [\"sample\", \"sample_short\"]\n    mean_slope_cols = [c for c in result.columns if \"mean_slope\" in c]\n    return result[id_cols + mean_slope_cols].reset_index(drop=True)\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.get_cumulated_heat_at_hours","title":"<code>get_cumulated_heat_at_hours(processparams=None, target_h=4, **kwargs)</code>","text":"<p>Get cumulated heat flow at specific age.</p> Source code in <code>calocem/measurement.py</code> <pre><code>def get_cumulated_heat_at_hours(\n    self,\n    processparams: Optional[ProcessingParameters] = None,\n    target_h: float = 4,\n    **kwargs,\n) -&gt; pd.DataFrame:\n    \"\"\"Get cumulated heat flow at specific age.\"\"\"\n    if \"cutoff_min\" in kwargs:\n        cutoff_min = kwargs[\"cutoff_min\"]\n        warnings.warn(\n            \"The cutoff_min parameter is deprecated. Use ProcessingParameters instead.\",\n            DeprecationWarning,\n            stacklevel=2,\n        )\n    else:\n        params = processparams or self.processparams\n        cutoff_min = params.cutoff.cutoff_min\n\n    return HeatCalculator.get_cumulated_heat_at_hours(\n        self._data, target_h, cutoff_min\n    )\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.get_data","title":"<code>get_data()</code>","text":"<p>Get the processed calorimetry data.</p> Source code in <code>calocem/measurement.py</code> <pre><code>def get_data(self) -&gt; pd.DataFrame:\n    \"\"\"Get the processed calorimetry data.\"\"\"\n    return self._data\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.get_deconvolution","title":"<code>get_deconvolution(processparams=None, target_col='normalized_heat_flow_w_g', age_col='time_s', regex=None, n_peaks=None, peak_shape='lognormal', baseline_mode=None, relative_intensity_upper_bounds=None, peak_width_upper_bounds=None, show_plot=False, ax=None)</code>","text":"<p>Fit a multi-peak deconvolution model to each sample.</p> Source code in <code>calocem/measurement.py</code> <pre><code>def get_deconvolution(\n    self,\n    processparams: Optional[ProcessingParameters] = None,\n    target_col: str = \"normalized_heat_flow_w_g\",\n    age_col: str = \"time_s\",\n    regex: Optional[str] = None,\n    n_peaks: Optional[int] = None,\n    peak_shape: str = \"lognormal\",\n    baseline_mode: Optional[str] = None,\n    relative_intensity_upper_bounds: Optional[list[float]] = None,\n    peak_width_upper_bounds: Optional[list[float]] = None,\n    show_plot: bool = False,\n    ax=None,\n) -&gt; pd.DataFrame:\n    \"\"\"Fit a multi-peak deconvolution model to each sample.\"\"\"\n    params = processparams or self.processparams\n    analyzer = DeconvolutionAnalyzer(params)\n    result = analyzer.get_deconvolution(\n        self._data,\n        target_col=target_col,\n        age_col=age_col,\n        regex=regex,\n        n_peaks=n_peaks,\n        peak_shape=peak_shape,\n        baseline_mode=baseline_mode,\n        relative_intensity_upper_bounds=relative_intensity_upper_bounds,\n        peak_width_upper_bounds=peak_width_upper_bounds,\n    )\n\n    if show_plot and not result.empty:\n        for sample, sample_data in SampleIterator.iter_samples(self._data, regex):\n            sample_short = pathlib.Path(str(sample)).stem\n            sample_result = result[result[\"sample_short\"] == sample_short]\n            if sample_result.empty:\n                continue\n\n            plot_data = sample_data[[age_col, target_col]].copy()\n            if params.cutoff.cutoff_min:\n                plot_data = plot_data[\n                    plot_data[age_col] &gt;= params.cutoff.cutoff_min * 60\n                ]\n            plot_data = plot_data.replace([np.inf, -np.inf], np.nan).dropna()\n            if plot_data.empty:\n                continue\n\n            x = plot_data[age_col].to_numpy(dtype=float)\n            y = plot_data[target_col].to_numpy(dtype=float)\n            x_logn = np.clip(x, 1e-12, None)\n            x_range = max(float(np.max(x) - np.min(x)), 1e-12)\n            x_scaled = 2.0 * (x - float(np.min(x))) / x_range - 1.0\n\n            created_ax = ax is None\n            if created_ax:\n                _, local_ax = plt.subplots(figsize=(7, 5))\n            else:\n                local_ax = ax\n\n            local_ax.plot(x, y, color=\"black\", linewidth=1.2, label=\"data\")\n\n            total_components = np.zeros_like(y)\n            shape = str(sample_result.iloc[0][\"peak_shape\"]).lower()\n            baseline = str(sample_result.iloc[0][\"baseline_mode\"]).lower()\n\n            for _, comp in sample_result.iterrows():\n                amplitude = float(comp[\"amplitude\"])\n                center = float(comp[\"center_time_s\"])\n                width = float(comp[\"width\"])\n\n                if shape == \"gaussian\":\n                    curve = analyzer._gaussian_peak(x, amplitude, center, width)\n                else:\n                    curve = analyzer._lognormal_peak(x_logn, amplitude, center, width)\n\n                total_components += curve\n                local_ax.plot(\n                    x,\n                    curve,\n                    linestyle=\"--\",\n                    linewidth=1,\n                    label=f\"component {int(comp['component'])}\",\n                )\n\n            baseline_constant = float(sample_result.iloc[0][\"baseline_constant\"])\n            baseline_slope = float(sample_result.iloc[0][\"baseline_slope\"])\n            if baseline == \"constant\" and not np.isnan(baseline_constant):\n                total_fit = total_components + baseline_constant\n            elif baseline == \"linear\" and not np.isnan(baseline_constant):\n                slope = 0.0 if np.isnan(baseline_slope) else baseline_slope\n                total_fit = total_components + baseline_constant + slope * x\n            elif baseline == \"chebyshev\":\n                cheb_coeffs = sample_result.iloc[0].get(\"baseline_cheb_coeffs\", None)\n                if isinstance(cheb_coeffs, str):\n                    import ast\n\n                    cheb_coeffs = ast.literal_eval(cheb_coeffs)\n                if cheb_coeffs is not None:\n                    baseline_curve = np.polynomial.chebyshev.chebval(\n                        x_scaled, np.array(cheb_coeffs, dtype=float)\n                    )\n                    total_fit = total_components + baseline_curve\n                else:\n                    total_fit = total_components\n            else:\n                total_fit = total_components\n\n            fit_r2 = sample_result[\"fit_r2\"].iloc[0]\n            local_ax.plot(\n                x,\n                total_fit,\n                color=\"tab:red\",\n                linewidth=1.5,\n                label=f\"fit (R\u00b2={fit_r2:.3f})\" if not pd.isna(fit_r2) else \"fit\",\n            )\n            local_ax.set_title(f\"Deconvolution: {sample_short}\")\n            local_ax.set_xlabel(age_col)\n            local_ax.set_ylabel(target_col)\n            local_ax.legend()\n\n            if created_ax:\n                plt.show()\n\n    return result\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.get_dormant_period_heatflow","title":"<code>get_dormant_period_heatflow(processparams=None, regex=None, cutoff_min=5, upper_dormant_thresh_w_g=0.002, plot_right_boundary=200000.0, prominence=0.001, show_plot=False)</code>","text":"<p>Get dormant period heat flow characteristics.</p> Source code in <code>calocem/measurement.py</code> <pre><code>def get_dormant_period_heatflow(\n    self,\n    processparams: Optional[ProcessingParameters] = None,\n    regex: Optional[str] = None,\n    cutoff_min: int = 5,\n    upper_dormant_thresh_w_g: float = 0.002,\n    plot_right_boundary: float = 2e5,\n    prominence: float = 1e-3,\n    show_plot: bool = False,\n) -&gt; pd.DataFrame:\n    \"\"\"Get dormant period heat flow characteristics.\"\"\"\n    params = processparams or self.processparams\n\n    # Get peaks first\n    peaks = self.get_peaks(params, regex=regex, show_plot=False)\n\n    # Analyze dormant period\n    analyzer = DormantPeriodAnalyzer(params)\n    dorm_hf = analyzer.get_dormant_period_heatflow(\n        self._data, peaks, regex, upper_dormant_thresh_w_g\n    )\n\n    if not dorm_hf.empty:\n        return dorm_hf\n    else:\n        return pd.DataFrame()\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.get_information","title":"<code>get_information()</code>","text":"<p>Get the measurement information/metadata.</p> Source code in <code>calocem/measurement.py</code> <pre><code>def get_information(self) -&gt; pd.DataFrame:\n    \"\"\"Get the measurement information/metadata.\"\"\"\n    return self._info\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.get_left_peak_inflection_tangent_intersection","title":"<code>get_left_peak_inflection_tangent_intersection(processparams=None, target_col='normalized_heat_flow_w_g', age_col='time_s', regex=None, n_peaks=None, peak_shape='lognormal', baseline_mode=None, deconvolution_results=None)</code>","text":"<p>Determine abscissa intersection from tangent at left-peak flank inflection.</p> Source code in <code>calocem/measurement.py</code> <pre><code>def get_left_peak_inflection_tangent_intersection(\n    self,\n    processparams: Optional[ProcessingParameters] = None,\n    target_col: str = \"normalized_heat_flow_w_g\",\n    age_col: str = \"time_s\",\n    regex: Optional[str] = None,\n    n_peaks: Optional[int] = None,\n    peak_shape: str = \"lognormal\",\n    baseline_mode: Optional[str] = None,\n    deconvolution_results: Optional[pd.DataFrame] = None,\n) -&gt; pd.DataFrame:\n    \"\"\"Determine abscissa intersection from tangent at left-peak flank inflection.\"\"\"\n    params = processparams or self.processparams\n    analyzer = DeconvolutionAnalyzer(params)\n\n    fit_results = deconvolution_results\n    if fit_results is None:\n        fit_results = self.get_deconvolution(\n            processparams=params,\n            target_col=target_col,\n            age_col=age_col,\n            regex=regex,\n            n_peaks=n_peaks,\n            peak_shape=peak_shape,\n            baseline_mode=baseline_mode,\n            show_plot=False,\n        )\n\n    return analyzer.get_left_peak_inflection_tangent_intersection(\n        self._data,\n        fit_results,\n        target_col=target_col,\n        age_col=age_col,\n        regex=regex,\n    )\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.get_mainpeak_params","title":"<code>get_mainpeak_params(processparams=None, target_col='normalized_heat_flow_w_g', age_col='time_s', show_plot=False, save_plot=False, plot_type='mean', regex=None, plotpath=None, ax=None, method='mean', monochrome=False, marker_size=1.0)</code>","text":"<pre><code>    Unified method for main-peak slope analysis.\n\n    Depending on ``method`` this either:\n    - ``\"mean\"`` (default): calculates maximum and mean slope onset analyses\n    - ``\"ascending\"``: calculates first ascending slope analysis via\n        ``get_first_ascending_slope_to_fraction``\n\n    Results are returned in a single DataFrame with all available slope values.\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>processparams</code> <code>ProcessingParameters</code> <p>Processing parameters, by default None</p> <code>None</code> <code>target_col</code> <code>str</code> <p>Column containing heat flow data. The default is 'normalized_heat_flow_w_g'.</p> <code>'normalized_heat_flow_w_g'</code> <code>age_col</code> <code>str</code> <p>Column containing time data. The default is 'time_s'.</p> <code>'time_s'</code> <code>show_plot</code> <code>bool</code> <p>Whether to plot the results</p> <code>False</code> <code>plot_type</code> <code>str</code> <p>Type of plot to show: 'max', 'mean', - 'max': Shows only maximum slope analysis plot - 'mean': Shows only mean slope (flank tangent) analysis plot</p> <code>'mean'</code> <code>regex</code> <code>str</code> <p>Regex to filter samples</p> <code>None</code> <code>plotpath</code> <code>Path</code> <p>Path to save plots</p> <code>None</code> <code>ax</code> <code>Axes</code> <p>Matplotlib axes to plot on</p> <code>None</code> <code>method</code> <code>str</code> <p>Slope analysis method to run: 'mean' or 'ascending'.</p> <code>'mean'</code> <code>monochrome</code> <code>bool</code> <p>If True, render all scatter/marker symbols on the analysis plot in black instead of their default colors. Default is False.</p> <code>False</code> <code>marker_size</code> <code>float</code> <p>Multiplicative scale factor applied to all scatter/marker sizes on the analysis plot. Default is 1.0.</p> <code>1.0</code> <p>Returns:</p> Type Description <code>DataFrame</code> <p>Comprehensive DataFrame with available slope and characteristic results.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; measurement = Measurement(folder=\"data/\")\n&gt;&gt;&gt; mainpeak_params = measurement.get_mainpeak_params(\n...     processparams=ProcessingParameters(),\n...     show_plot=False,\n...     plot_type=\"mean\",\n...     method=\"mean\",\n... )\n</code></pre> Source code in <code>calocem/measurement.py</code> <pre><code>def get_mainpeak_params(\n    self,\n    processparams: Optional[ProcessingParameters] = None,\n    target_col: str = \"normalized_heat_flow_w_g\",\n    age_col: str = \"time_s\",\n    show_plot: bool = False,\n    save_plot: bool = False,\n    plot_type: str = \"mean\",\n    regex: Optional[str] = None,\n    plotpath: Optional[pathlib.Path] = None,\n    ax=None,\n    method: str = \"mean\",\n    monochrome: bool = False,\n    marker_size: float = 1.0,\n) -&gt; pd.DataFrame:\n    \"\"\"\n            Unified method for main-peak slope analysis.\n\n            Depending on ``method`` this either:\n            - ``\"mean\"`` (default): calculates maximum and mean slope onset analyses\n            - ``\"ascending\"``: calculates first ascending slope analysis via\n                ``get_first_ascending_slope_to_fraction``\n\n            Results are returned in a single DataFrame with all available slope values.\n\n    Parameters\n    ----------\n    processparams : ProcessingParameters, optional\n        Processing parameters, by default None\n    target_col : str\n        Column containing heat flow data. The default is 'normalized_heat_flow_w_g'.\n    age_col : str\n        Column containing time data. The default is 'time_s'.\n    show_plot : bool\n        Whether to plot the results\n    plot_type : str\n        Type of plot to show: 'max', 'mean',\n        - 'max': Shows only maximum slope analysis plot\n        - 'mean': Shows only mean slope (flank tangent) analysis plot\n    regex : str, optional\n        Regex to filter samples\n    plotpath : pathlib.Path, optional\n        Path to save plots\n    ax : matplotlib.axes.Axes, optional\n        Matplotlib axes to plot on\n    method : str\n        Slope analysis method to run: 'mean' or 'ascending'.\n    monochrome : bool\n        If True, render all scatter/marker symbols on the analysis plot in\n        black instead of their default colors. Default is False.\n    marker_size : float\n        Multiplicative scale factor applied to all scatter/marker sizes on\n        the analysis plot. Default is 1.0.\n\n    Returns\n    -------\n    pd.DataFrame\n        Comprehensive DataFrame with available slope and characteristic results.\n\n    Examples\n    --------\n    &gt;&gt;&gt; measurement = Measurement(folder=\"data/\")\n    &gt;&gt;&gt; mainpeak_params = measurement.get_mainpeak_params(\n    ...     processparams=ProcessingParameters(),\n    ...     show_plot=False,\n    ...     plot_type=\"mean\",\n    ...     method=\"mean\",\n    ... )\n    \"\"\"\n    params = processparams or self.processparams\n\n    valid_methods = [\"mean\", \"ascending\"]\n    if method not in valid_methods:\n        raise ValueError(f\"method must be one of {valid_methods}, got '{method}'\")\n\n    if method == \"mean\":\n        max_slope_results = self._calculate_max_slope_analysis(\n            params,\n            target_col,\n            age_col,\n            regex,\n        )\n\n        mean_slope_results = self._calculate_mean_slope_analysis(\n            params,\n            target_col,\n            age_col,\n            regex,\n        )\n    elif method == \"ascending\":\n        max_slope_results = pd.DataFrame()\n        mean_slope_results = self._calculate_first_ascending_slope_analysis(\n            params,\n            target_col,\n            age_col,\n            regex,\n        )\n\n    dormant_minimum_heatflow = self.get_dormant_period_heatflow(\n        params, regex, show_plot=False\n    )\n\n    astm_values = self.get_astm_c1679_characteristics(params, individual=True, show_plot=False, regex=regex)\n\n    # Merge results into comprehensive DataFrame\n    combined_results = self._merge_slope_results(\n        max_slope_results, mean_slope_results, dormant_minimum_heatflow, astm_values\n    )\n\n    # Plot if requested\n    if (\n        method == \"mean\"\n        and (show_plot or save_plot)\n        and not (mean_slope_results.empty or max_slope_results.empty)\n    ):\n        self._plot_combined_slope_analysis(\n            combined_results,\n            params,\n            target_col,\n            age_col,\n            plot_type,\n            regex,\n            plotpath if save_plot else None,\n            ax,\n            show_plot=show_plot,\n            monochrome=monochrome,\n            marker_size=marker_size,\n        )\n        # if not ax:\n        #     plt.show()\n            # if (save_plot and show_plot) and plotpath:\n            #     plt.savefig(plotpath)\n            #     plt.show()\n            # elif show_plot:\n            #     plt.show()\n            # elif save_plot:\n            #     plt.savefig(plotpath)\n\n    elif (\n        method == \"ascending\"\n        and (show_plot or save_plot)\n        and not mean_slope_results.empty\n    ):\n        self._plot_ascending_slope_analysis(\n            combined_results,\n            params,\n            target_col,\n            age_col,\n            regex,\n            plotpath if save_plot else None,\n            ax,\n            show_plot=show_plot,\n            monochrome=monochrome,\n            marker_size=marker_size,\n        )\n\n    elif method == \"mean\" and mean_slope_results.empty:\n        # logger.warning(\"No slope analysis results to plot.\")\n        print(\"No mean slope analysis obtained - check the processing parameters.\")\n\n    elif method == \"mean\" and max_slope_results.empty:\n        print(\n            \"No maximum slope analysis obtained - check the processing parameters.\"\n        )\n\n    return combined_results\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.get_maximum_slope","title":"<code>get_maximum_slope(processparams=None, target_col='normalized_heat_flow_w_g', age_col='time_s', show_plot=False, regex=None, ax=None, save_path=None, xunit='s')</code>","text":"<p>Find the point in time of the maximum slope.</p> <p>Wrapper around :meth:<code>get_mainpeak_params</code> returning only the <code>max_slope</code> columns together with sample identifiers.</p> Source code in <code>calocem/measurement.py</code> <pre><code>def get_maximum_slope(\n    self,\n    processparams: Optional[ProcessingParameters] = None,\n    target_col: str = \"normalized_heat_flow_w_g\",\n    age_col: str = \"time_s\",\n    show_plot: bool = False,\n    regex: Optional[str] = None,\n    ax=None,\n    save_path: Optional[pathlib.Path] = None,\n    xunit: str = \"s\",\n) -&gt; pd.DataFrame:\n    \"\"\"Find the point in time of the maximum slope.\n\n    Wrapper around :meth:`get_mainpeak_params` returning only the\n    ``max_slope`` columns together with sample identifiers.\n    \"\"\"\n    result = self.get_mainpeak_params(\n        processparams=processparams,\n        target_col=target_col,\n        age_col=age_col,\n        show_plot=show_plot,\n        plot_type=\"max\",\n        regex=regex,\n        ax=ax,\n    )\n    if result.empty:\n        return result\n    id_cols = [\"sample\", \"sample_short\"]\n    max_slope_cols = [c for c in result.columns if \"max_slope\" in c]\n    return result[id_cols + max_slope_cols].reset_index(drop=True)\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.get_metadata","title":"<code>get_metadata()</code>","text":"<p>Get added metadata and the ID column name.</p> Source code in <code>calocem/measurement.py</code> <pre><code>def get_metadata(self) -&gt; tuple:\n    \"\"\"Get added metadata and the ID column name.\"\"\"\n    return self._metadata, self._metadata_id\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.get_peak_onset_via_max_slope","title":"<code>get_peak_onset_via_max_slope(processparams=None, show_plot=False, ax=None, regex=None, age_col='time_s', target_col='normalized_heat_flow_w_g', time_discarded_s=900, save_path=None, xscale='linear', xunit='s', intersection='dormant_hf')</code>","text":"<p>Get reaction onset via maximum slope intersection method.</p> <p>This is a wrapper around get_peak_onset_via_slope for backward compatibility. Returns only the max slope related columns for compatibility.</p> Source code in <code>calocem/measurement.py</code> <pre><code>def get_peak_onset_via_max_slope(\n    self,\n    processparams: Optional[ProcessingParameters] = None,\n    show_plot: bool = False,\n    ax=None,\n    regex: Optional[str] = None,\n    age_col: str = \"time_s\",\n    target_col: str = \"normalized_heat_flow_w_g\",\n    time_discarded_s: float = 900,\n    save_path: Optional[pathlib.Path] = None,\n    xscale: str = \"linear\",\n    xunit: str = \"s\",\n    intersection: str = \"dormant_hf\",\n):\n    \"\"\"\n    Get reaction onset via maximum slope intersection method.\n\n    This is a wrapper around get_peak_onset_via_slope for backward compatibility.\n    Returns only the max slope related columns for compatibility.\n    \"\"\"\n    full_results = self.get_mainpeak_params(\n        processparams=processparams,\n        target_col=target_col,\n        age_col=age_col,\n        show_plot=show_plot,\n        regex=regex,\n        ax=ax,\n        plot_type=\"max\",\n\n        #time_discarded_s=time_discarded_s,\n        #intersection=intersection,\n        #xunit=xunit,\n    )\n\n    if full_results.empty:\n        return full_results\n\n    # Extract only max slope related columns for backward compatibility\n    # max_slope_cols = [\n    #     col\n    #     for col in full_results.columns\n    #     if col.startswith(\"max_slope_\") or col in [\"sample\", \"sample_short\"]\n    # ]\n\n    # result = full_results[max_slope_cols].copy()\n\n    # Rename columns to match old API\n    # column_mapping = {\n    #     \"onset_time_s_from_max_slope\": \"onset_time_s\",\n    #     \"max_slope_onset_time_min\": \"onset_time_min\",\n    #     \"max_slope_value\": \"maximum_slope\",\n    #     \"max_slope_time_s\": \"maximum_slope_time_s\",\n    # }\n\n    # for old_name, new_name in column_mapping.items():\n    #     if old_name in result.columns:\n    #         result = result.rename(columns={old_name: new_name})\n\n    return full_results\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.get_peak_onsets","title":"<code>get_peak_onsets(target_col='normalized_heat_flow_w_g', age_col='time_s', time_discarded_s=900, rolling=1, gradient_threshold=0.0005, show_plot=False, exclude_discarded_time=False, regex=None, ax=None)</code>","text":"<p>Get peak onsets based on gradient threshold.</p> Source code in <code>calocem/measurement.py</code> <pre><code>def get_peak_onsets(\n    self,\n    target_col: str = \"normalized_heat_flow_w_g\",\n    age_col: str = \"time_s\",\n    time_discarded_s: float = 900,\n    rolling: int = 1,\n    gradient_threshold: float = 0.0005,\n    show_plot: bool = False,\n    exclude_discarded_time: bool = False,\n    regex: Optional[str] = None,\n    ax=None,\n):\n    \"\"\"Get peak onsets based on gradient threshold.\"\"\"\n    analyzer = OnsetAnalyzer(self.processparams)\n    return analyzer.get_peak_onsets(\n        self._data,\n        target_col,\n        age_col,\n        time_discarded_s,\n        rolling,\n        gradient_threshold,\n        exclude_discarded_time,\n        regex,\n    )\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.get_peaks","title":"<code>get_peaks(processparams=None, target_col='normalized_heat_flow_w_g', regex=None, cutoff_min=None, show_plot=True, plt_right_s=200000.0, plt_top=0.01, ax=None, xunit='s', plot_labels=None, xmarker=False)</code>","text":"<p>Get DataFrame of peak characteristics.</p> Source code in <code>calocem/measurement.py</code> <pre><code>def get_peaks(\n    self,\n    processparams: Optional[ProcessingParameters] = None,\n    target_col: str = \"normalized_heat_flow_w_g\",\n    regex: Optional[str] = None,\n    cutoff_min: Optional[float] = None,  # Deprecated parameter\n    show_plot: bool = True,\n    plt_right_s: float = 2e5,\n    plt_top: float = 1e-2,\n    ax=None,\n    xunit: str = \"s\",\n    plot_labels: Optional[bool] = None,\n    xmarker: bool = False,\n) -&gt; pd.DataFrame:\n    \"\"\"Get DataFrame of peak characteristics.\"\"\"\n    if cutoff_min is not None:\n        warnings.warn(\n            \"The cutoff_min parameter is deprecated. Use ProcessingParameters instead.\",\n            DeprecationWarning,\n            stacklevel=2,\n        )\n\n    params = processparams or self.processparams\n    analyzer = PeakAnalyzer(params)\n    peaks_df = analyzer.get_peaks(self._data, target_col, regex)\n\n    if show_plot and not peaks_df.empty:\n        # Simple plotting implementation\n        for sample, sample_data in SampleIterator.iter_samples(self._data, regex):\n            sample_peaks = peaks_df[\n                peaks_df[\"sample_short\"] == pathlib.Path(str(sample)).stem\n            ]\n            if not sample_peaks.empty:\n                # Locate peak positions within sample_data by matching time_s values\n                peak_indices = np.array([\n                    sample_data.index.get_loc(\n                        sample_data[\"time_s\"].sub(t).abs().idxmin()\n                    )\n                    for t in sample_peaks[\"time_s\"]\n                ])\n                self._plotter.plot_peaks(\n                    sample_data.reset_index(drop=True),\n                    peak_indices,\n                    str(sample),\n                    ax,\n                    \"time_s\",\n                    target_col,\n                )\n\n    return peaks_df\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.get_sample_names","title":"<code>get_sample_names()</code>","text":"<p>Get list of sample names.</p> Source code in <code>calocem/measurement.py</code> <pre><code>def get_sample_names(self) -&gt; list:\n    \"\"\"Get list of sample names.\"\"\"\n    return [\n        pathlib.Path(str(sample)).stem\n        for sample, _ in SampleIterator.iter_samples(self._data)\n    ]\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.normalize_sample_to_mass","title":"<code>normalize_sample_to_mass(sample_short, mass_g, show_info=True)</code>","text":"<p>Normalize heat flow values to a specific mass.</p> Source code in <code>calocem/measurement.py</code> <pre><code>def normalize_sample_to_mass(\n    self, sample_short: str, mass_g: float, show_info: bool = True\n):\n    \"\"\"Normalize heat flow values to a specific mass.\"\"\"\n    self._data = DataNormalizer.normalize_sample_to_mass(\n        self._data, sample_short, mass_g, show_info\n    )\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.plot","title":"<code>plot(t_unit='h', y='normalized_heat_flow_w_g', y_unit_milli=True, regex=None, show_info=True, ax=None)</code>","text":"<p>Plot the calorimetry data.</p> Source code in <code>calocem/measurement.py</code> <pre><code>def plot(\n    self,\n    t_unit: str = \"h\",\n    y: str = \"normalized_heat_flow_w_g\",\n    y_unit_milli: bool = True,\n    regex: Optional[str] = None,\n    show_info: bool = True,\n    ax=None,\n):\n    \"\"\"Plot the calorimetry data.\"\"\"\n    return self._plotter.plot_data(\n        self._data, t_unit, y, y_unit_milli, regex, show_info, ax\n    )\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.plot_by_category","title":"<code>plot_by_category(categories, t_unit='h', y='normalized_heat_flow_w_g', y_unit_milli=True)</code>","text":"<p>Plot data by metadata categories.</p> Source code in <code>calocem/measurement.py</code> <pre><code>def plot_by_category(\n    self,\n    categories: str,\n    t_unit: str = \"h\",\n    y: str = \"normalized_heat_flow_w_g\",\n    y_unit_milli: bool = True,\n):\n    \"\"\"Plot data by metadata categories.\"\"\"\n    # Simplified implementation - would need full metadata integration\n    logger.warning(\n        \"plot_by_category requires metadata integration - not fully implemented\"\n    )\n    yield from []\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.plot_heatflow_with_gradient","title":"<code>plot_heatflow_with_gradient(processparams=None, t_unit='h', target_col='normalized_heat_flow_w_g', age_col='time_s', y_unit_milli=True, gradient_unit_milli=True, gradient_color='orange', align_zeros=True, show_zero_line=False, grid=False, regex=None, show_info=True, ax=None)</code>","text":"<p>Plot heat flow curve(s) with their gradient on a secondary y-axis.</p> <p>The gradient is computed using the smoothing settings (rolling mean, median filter, spline interpolation) from <code>processparams</code> (or the instance's processparams when not provided).</p> <p>Returns:</p> Type Description <code>(ax, ax_grad) : tuple of matplotlib.axes.Axes</code> <p>Primary (heat flow) and secondary (gradient) axes.</p> Source code in <code>calocem/measurement.py</code> <pre><code>def plot_heatflow_with_gradient(\n    self,\n    processparams: Optional[ProcessingParameters] = None,\n    t_unit: str = \"h\",\n    target_col: str = \"normalized_heat_flow_w_g\",\n    age_col: str = \"time_s\",\n    y_unit_milli: bool = True,\n    gradient_unit_milli: bool = True,\n    gradient_color: Optional[str] = \"orange\",\n    align_zeros: bool = True,\n    show_zero_line: bool = False,\n    grid: bool = False,\n    regex: Optional[str] = None,\n    show_info: bool = True,\n    ax=None,\n):\n    \"\"\"Plot heat flow curve(s) with their gradient on a secondary y-axis.\n\n    The gradient is computed using the smoothing settings (rolling mean,\n    median filter, spline interpolation) from ``processparams`` (or the\n    instance's processparams when not provided).\n\n    Returns\n    -------\n    (ax, ax_grad) : tuple of matplotlib.axes.Axes\n        Primary (heat flow) and secondary (gradient) axes.\n    \"\"\"\n    params = processparams or self.processparams\n    return self._plotter.plot_heatflow_with_gradient(\n        self._data,\n        params,\n        t_unit=t_unit,\n        target_col=target_col,\n        age_col=age_col,\n        y_unit_milli=y_unit_milli,\n        gradient_unit_milli=gradient_unit_milli,\n        gradient_color=gradient_color,\n        align_zeros=align_zeros,\n        show_zero_line=show_zero_line,\n        grid=grid,\n        regex=regex,\n        show_info=show_info,\n        ax=ax,\n    )\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.remove_pickle_files","title":"<code>remove_pickle_files()</code>","text":"<p>Remove pickle cache files.</p> Source code in <code>calocem/measurement.py</code> <pre><code>def remove_pickle_files(self):\n    \"\"\"Remove pickle cache files.\"\"\"\n    self._data_persistence.remove_pickle_files()\n</code></pre>"},{"location":"reference.html#calocem.measurement.Measurement.undo_average_by_metadata","title":"<code>undo_average_by_metadata()</code>","text":"<p>Restore the original per-sample data after average_by_metadata.</p> Source code in <code>calocem/measurement.py</code> <pre><code>def undo_average_by_metadata(self) -&gt; None:\n    \"\"\"Restore the original per-sample data after average_by_metadata.\"\"\"\n    if not hasattr(self, \"_data_before_average\"):\n        raise ValueError(\n            \"No averaged data to undo. Call average_by_metadata() first.\"\n        )\n    self._data = self._data_before_average\n    del self._data_before_average\n</code></pre>"},{"location":"tian.html","title":"Tian Correction","text":""},{"location":"tian.html#example-workflow","title":"Example Workflow","text":""},{"location":"tian.html#load-the-data","title":"Load the Data","text":"<p>For fast reactions, e.g., reactions occurring during the first minutes of cement hydration, the thermal inertia of the calorimeter significantly broadens the heat flow signal.  If the characteristic time constants are determined experimentally, a Tian correction can be applied to the heat flow data.</p> <p>First we load the data.</p> <pre><code>from pathlib import Path\n\nimport matplotlib.pyplot as plt\n\nfrom calocem import Measurement, ProcessingParameters\n\ndatapath = Path(__file__).parent / \"calo_data\"\n\n# load experimental data\ntam = Measurement(\n    folder=datapath,\n    regex=r\".*file1.csv\",\n    show_info=True,\n    auto_clean=False,\n    cold_start=True,\n)\n</code></pre> <p>In the next step, we need to define a few parameters which are necessary for the Tian correction.  Therefore we create a ProcessingParameters object which we call <code>processparams</code> in this case. The <code>processparams</code> object has a number of attributes which we can define. First, we define two time constants <code>tau1</code> and <code>tau2</code>. The numeric value needs to be determined experimentally.</p> <pre><code># Set Processing Parameters\nprocessparams = ProcessingParameters()\nprocessparams.time_constants.tau1 = 240\nprocessparams.time_constants.tau2 = 80\nprocessparams.median_filter.apply = True\nprocessparams.median_filter.size = 15\nprocessparams.spline_interpolation.apply = True\nprocessparams.spline_interpolation.smoothing_1st_deriv = 1e-10\nprocessparams.spline_interpolation.smoothing_2nd_deriv = 1e-10\n</code></pre> <p>Next we apply the Tian correction by calling the method <code>apply_tian_correction()</code>. We pass the <code>processparams</code> object defined above to the method.</p> <pre><code># apply tian correction\ntam.apply_tian_correction(\n    processparams=processparams,\n)\n</code></pre> <p>Finally, we can get the Pandas dataframe containing the calorimetric data by calling <code>get_data()</code>. Using the <code>df</code> DataFrame we can plot the calorimetry data using well-known Matplotlib methods.</p> <pre><code>df = tam.get_data()\n\n# plot corrected and uncorrected data\nfig, ax = plt.subplots()\nax.plot(\n    df[\"time_s\"] / 60,\n    df[\"normalized_heat_flow_w_g\"],\n    linestyle=\"--\",\n    label=\"sample\"\n    )\nax.plot(\n    df[\"time_s\"] / 60,\n    df[\"normalized_heat_flow_w_g_tian\"],\n    color=ax.get_lines()[-1].get_color(),\n    label=\"Tian corrected\"\n    )\nax.set_xlim(0, 15)\nax.set_xlabel(\"Time (min)\")\nax.set_ylabel(\"Normalized Heat Flow (W/g)\")\nax.legend()\nplt.show()\n</code></pre> <p></p>"},{"location":"tian.html#one-or-two-tau-values","title":"One or Two Tau Values","text":"<p>If only one Tau value is defined, the correction algorithm will only consider this \\(\\tau\\) value and the data will be corrected according to </p> \\[ P(t) = \\varepsilon \\left[ U(t) + \\tau \\frac{dU(t)}{dt} \\right] \\] <p>If two values for \\(\\tau\\) are provided, the data will be corrected considering both values.</p> \\[ P(t) = \\varepsilon \\left[ U(t) + (\\tau_1+\\tau_2) \\frac{dU(t)}{dt} + \\tau_1\\tau_2 \\frac{d^2U}{dt^2} \\right] \\] <p>The actual implementation of the correction algorithm is not based on the voltage \\(U\\) but on the heat flow.  In most cases, the exported data does not contain the raw voltage data but the heat flow data which has been obtained in the instrument software with the experimentally determined value for \\(\\varepsilon\\).</p> <p>Therefore, the second equation reads like</p> \\[ \\dot{Q}_{Tian}(t) =  \\dot{Q}(t) + (\\tau_1+\\tau_2) \\frac{\\dot{Q}}{dt} + \\tau_1\\tau_2 \\frac{d^2\\dot{Q}}{dt^2}  \\] <p>In practical terms, if only the attribute tau1 is set, only the first derivative of the heat flow will be considered.</p> <pre><code># Set Processing Parameters\nprocessparams = ProcessingParameters()\nprocessparams.time_constants.tau1 = 300\n</code></pre> <p>The difference between having one or two tau constants can be seen in the following plot. In general, having two tau constants renders the signal even more narrow.</p> <p></p>"},{"location":"tian.html#smoothing-the-data","title":"Smoothing the Data","text":""},{"location":"tian.html#no-smoothing","title":"No smoothing","text":"<p>It is important to smoothen the data.  Otherwise, small noise in the raw heat flow data will lead to significant noise especially in the second derivative. By default the smoothing is not applied.  Here, we explicitly set the attributes to <code>False</code> and repeat the analysis as shown above. The results demonstrates the noise in the data which originates from tiny fluctuations which result in significant noise in the second derivative.</p> <pre><code># Set Processing Parameters\nprocessparams.median_filter.apply = False\nprocessparams.spline_interpolation.apply = False\n</code></pre> <p></p>"},{"location":"tian.html#only-median-filter","title":"Only Median Filter","text":"<p>Here is the result of only applying a median filter with a size of 15.</p> <p><pre><code># Set Processing Parameters\nprocessparams.median_filter.apply = True\nprocessparams.median_filter.size = 15\nprocessparams.spline_interpolation.apply = False\n</code></pre> </p>"},{"location":"tian.html#only-spline-smoothing","title":"Only Spline Smoothing","text":"<p>Here is the result of only applying a Univariate Spline with smoothing of 1e-10 for both the first and the second derivative. The combination of a median filter and spline smoothing reliably delivers smooth data without introducing artifacts or significant line broadening.</p> <p><pre><code># Set Processing Parameters\nprocessparams.median_filter.apply = False\nprocessparams.spline_interpolation.apply = True\nprocessparams.spline_interpolation.smoothing_1st_deriv = 1e-10\nprocessparams.spline_interpolation.smoothing_2nd_deriv = 1e-10\n</code></pre> </p>"}]}